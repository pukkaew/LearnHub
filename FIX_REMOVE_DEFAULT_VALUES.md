# แก้ไข: ลบค่า Default ที่ไม่ควรมี

**วันที่:** 17 พฤศจิกายน 2025

---

## 🔍 ปัญหา

**ถ้าไม่ได้กรอกหรือไม่ได้เลือก ก็ไม่ควรมีค่าขึ้นมาเลย**

### ค่าที่ปรากฏโดยอัตโนมัติ:
- ❌ **Passing Score = 70%** (ไม่ได้กรอกแต่ขึ้นมาเอง)
- ❌ **Max Attempts = 3 ครั้ง** (ไม่ได้กรอกแต่ขึ้นมาเอง)

### ที่มาของปัญหา:

#### 1. ฟอร์มมี default value
**ไฟล์:** `views/courses/create.ejs`
```html
<input type="number" id="new_passing_score" value="70">
<input type="number" id="new_max_attempts" value="3">
```

#### 2. JavaScript มี fallback value
**ไฟล์:** `public/js/course-wizard.js`
```javascript
formData.passing_score = ... || 70;   // ← ถ้าไม่มีค่าใช้ 70
formData.max_attempts = ... || 3;     // ← ถ้าไม่มีค่าใช้ 3
```

---

## ✅ การแก้ไข

### 1. ลบ default value จากฟอร์ม

**ไฟล์:** `views/courses/create.ejs` (line 530, 536)

**เดิม:**
```html
<input type="number" id="new_passing_score" value="70">
<input type="number" id="new_max_attempts" value="3">
```

**ใหม่:**
```html
<input type="number" id="new_passing_score" placeholder="เช่น 70">
<p class="text-xs text-gray-500 mt-1">ปล่อยว่างถ้าไม่ต้องการระบุ</p>

<input type="number" id="new_max_attempts" placeholder="เช่น 3">
<p class="text-xs text-gray-500 mt-1">ปล่อยว่างถ้าไม่ต้องการระบุ</p>
```

**การเปลี่ยนแปลง:**
- ✅ ลบ `value="70"` และ `value="3"`
- ✅ เพิ่ม `placeholder` เป็นตัวอย่าง
- ✅ เพิ่มคำอธิบาย "ปล่อยว่างถ้าไม่ต้องการระบุ"

### 2. แก้ JavaScript ให้ใช้ null แทนค่า default

**ไฟล์:** `public/js/course-wizard.js` (line 881-886)

**เดิม:**
```javascript
formData.passing_score = coursePassingScore ||
                         parseInt(document.getElementById('new_passing_score')?.value) ||
                         70;  // ← ค่า default
formData.max_attempts = courseMaxAttempts ||
                        parseInt(document.getElementById('new_max_attempts')?.value) ||
                        3;   // ← ค่า default
```

**ใหม่:**
```javascript
formData.passing_score = coursePassingScore ||
                         parseInt(document.getElementById('new_passing_score')?.value) ||
                         null;  // ← ไม่มีค่า default
formData.max_attempts = courseMaxAttempts ||
                        parseInt(document.getElementById('new_max_attempts')?.value) ||
                        null;  // ← ไม่มีค่า default
```

**การเปลี่ยนแปลง:**
- ✅ เปลี่ยนจาก `70` เป็น `null`
- ✅ เปลี่ยนจาก `3` เป็น `null`

### 3. แก้การแสดงผลให้แสดง "ไม่ระบุ" ถ้าเป็น null

**ไฟล์:** `views/courses/detail.ejs` (line 503-512)

**เดิม:**
```javascript
document.getElementById('sidebar-passing-score').textContent =
    course.passing_score ? `${course.passing_score}%` : 'ไม่ระบุ';

document.getElementById('sidebar-max-attempts').textContent =
    course.max_attempts ? `${course.max_attempts} ครั้ง` : 'ไม่จำกัด';
```

**ใหม่:**
```javascript
document.getElementById('sidebar-passing-score').textContent =
    (course.passing_score !== null && course.passing_score !== undefined)
        ? `${course.passing_score}%`
        : 'ไม่ระบุ';

document.getElementById('sidebar-max-attempts').textContent =
    (course.max_attempts !== null && course.max_attempts !== undefined)
        ? `${course.max_attempts} ครั้ง`
        : 'ไม่ระบุ';
```

**การเปลี่ยนแปลง:**
- ✅ เช็คว่าเป็น `null` หรือ `undefined` อย่างชัดเจน
- ✅ ป้องกัน edge case ที่ค่าเป็น 0
- ✅ แสดง "ไม่ระบุ" สำหรับทั้ง passing_score และ max_attempts

---

## 📊 ผลลัพธ์

### ก่อนแก้ไข ❌

**ถ้าไม่กรอก:**
```
เกณฑ์ผ่าน: 70%        ← ไม่ได้กรอกแต่ขึ้นมาเอง!
ทำได้สูงสุด: 3 ครั้ง   ← ไม่ได้กรอกแต่ขึ้นมาเอง!
```

### หลังแก้ไข ✅

**ถ้าไม่กรอก:**
```
เกณฑ์ผ่าน: ไม่ระบุ      ← ถูกต้อง!
ทำได้สูงสุด: ไม่ระบุ    ← ถูกต้อง!
```

**ถ้ากรอก 80 และ 5:**
```
เกณฑ์ผ่าน: 80%         ← แสดงค่าที่กรอก
ทำได้สูงสุด: 5 ครั้ง    ← แสดงค่าที่กรอก
```

---

## 🧪 วิธีทดสอบ

### 1. ล้างข้อมูล
```bash
node clean_all_courses_data.js
```

### 2. สร้างหลักสูตรใหม่
เข้า http://localhost:3000/courses/create

### 3. ทดสอบกรณีต่างๆ

#### กรณีที่ 1: **ไม่กรอก** passing_score และ max_attempts
**ผลที่คาดหวัง:**
- Database: `passing_score = null`, `max_attempts = null`
- หน้า Detail: แสดง "ไม่ระบุ" ทั้งสองฟิลด์

#### กรณีที่ 2: **กรอก** passing_score = 80, max_attempts = 5
**ผลที่คาดหวัง:**
- Database: `passing_score = 80`, `max_attempts = 5`
- หน้า Detail: แสดง "80%" และ "5 ครั้ง"

#### กรณีที่ 3: **กรอกแค่** passing_score = 90
**ผลที่คาดหวัง:**
- Database: `passing_score = 90`, `max_attempts = null`
- หน้า Detail: แสดง "90%" และ "ไม่ระบุ"

---

## 📁 ไฟล์ที่แก้ไข

### 1. `views/courses/create.ejs`
- **Line 530:** ลบ `value="70"` เพิ่ม `placeholder="เช่น 70"`
- **Line 532:** เพิ่มคำอธิบาย "ปล่อยว่างถ้าไม่ต้องการระบุ"
- **Line 536:** ลบ `value="3"` เพิ่ม `placeholder="เช่น 3"`
- **Line 538:** เพิ่มคำอธิบาย "ปล่อยว่างถ้าไม่ต้องการระบุ"

### 2. `public/js/course-wizard.js`
- **Line 883:** เปลี่ยน `70` เป็น `null`
- **Line 886:** เปลี่ยน `3` เป็น `null`

### 3. `views/courses/detail.ejs`
- **Line 503-506:** แก้การแสดงผล passing_score
- **Line 509-512:** แก้การแสดงผล max_attempts

---

## 📝 หมายเหตุ

### ข้อดีของการแก้ไข:
1. ✅ **ความชัดเจน:** ถ้าไม่กรอกจะแสดง "ไม่ระบุ" ไม่ใช่ค่าสุ่ม
2. ✅ **ความถูกต้อง:** ข้อมูลใน database สะท้อนความจริง (null = ไม่ได้กรอก)
3. ✅ **UX ที่ดีขึ้น:** ผู้ใช้เห็น placeholder เป็นตัวอย่าง
4. ✅ **ยืดหยุ่น:** สามารถกรอกหรือไม่กรอกก็ได้

### ข้อควรระวัง:
- ⚠️ ถ้า **ต้องการให้มีค่า default** ให้กลับไปใช้ `value="70"` และ `value="3"`
- ⚠️ ถ้า **ต้องการบังคับให้กรอก** ให้เพิ่ม validation ใน validateStep4()

---

**สถานะ:** ✅ **เสร็จสมบูรณ์**
**ทดสอบ:** พร้อมใช้งาน - ลองสร้างหลักสูตรใหม่โดย**ไม่กรอก**ค่าเหล่านี้
