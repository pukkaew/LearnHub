# 🌐 คู่มือการทดสอบระบบ Tests ผ่านหน้าจอ HTML

**วันที่:** 23 พฤศจิกายน 2025
**สถานะ:** ✅ **พร้อมทดสอบ - Model และ Controller แก้ไขเรียบร้อยแล้ว**

---

## 🚀 การเริ่มต้น - เปิด Server

### 1. เปิด Server
```bash
node server.js
```

**Server จะทำงานที่:** http://localhost:3000

**ตรวจสอบว่า Server ทำงาน:**
- เห็นข้อความ: `🚀 Rukchai Hongyen LearnHub Server is running on port 3000`
- เห็นข้อความ: `Connected to MSSQL database`

---

## 📍 URL สำหรับเข้าถึงหน้าต่าง ๆ

### หน้าจอหลักของระบบ Tests:

| หน้าจอ | URL | สิทธิ์การเข้าถึง |
|--------|-----|------------------|
| **รายการข้อสอบทั้งหมด** | http://localhost:3000/tests | ต้อง Login |
| **สร้างข้อสอบใหม่** | http://localhost:3000/tests/create | Admin, Instructor |
| **ดูรายละเอียดข้อสอบ** | http://localhost:3000/tests/:test_id | ต้อง Login |
| **ทำข้อสอบ** | http://localhost:3000/tests/:test_id/:attempt_id/taking | ต้อง Login |

**หมายเหตุ:** `:test_id` = หมายเลข ID ของข้อสอบ (เช่น 1, 2, 3, ...)

---

## 🔑 การ Login เข้าสู่ระบบ

**ก่อนทดสอบต้อง Login ก่อน!**

1. เปิดเบราว์เซอร์ไปที่: http://localhost:3000/login
2. Login ด้วย user ที่มีสิทธิ์:
   - **Admin** - สามารถทำทุกอย่างได้
   - **Instructor** - สร้าง/แก้ไขข้อสอบได้
   - **User/Student** - ดูและทำข้อสอบได้

**ตัวอย่าง User ที่มีในระบบ:**
- User ID: 17 (Instructor) - ใช้สำหรับสร้าง/แก้ไขข้อสอบ

---

## ✅ ขั้นตอนการทดสอบผ่านหน้าจอ HTML

### 📝 Test Case 1: ดูรายการข้อสอบทั้งหมด (Read - List)

**Steps:**
1. เปิด: http://localhost:3000/tests
2. Login เข้าสู่ระบบ (ถ้ายังไม่ได้ login)
3. ควรเห็นรายการข้อสอบทั้งหมดที่มีในระบบ

**สิ่งที่ควรเห็น:**
- ✅ รายการข้อสอบแสดงผล (จากตาราง `tests`)
- ✅ แสดงชื่อข้อสอบ (`title`)
- ✅ แสดงประเภทข้อสอบ (`type`)
- ✅ แสดงสถานะ (`status`: Active, Inactive, Draft)
- ✅ แสดงคะแนนรวม (`total_marks`)
- ✅ แสดงเวลาทำข้อสอบ (`time_limit`)

**สิ่งที่ต้องตรวจสอบ:**
- ❌ ไม่เห็น Error message
- ❌ ไม่มี undefined หรือ null
- ❌ ไม่มี column name mismatch errors

---

### 📝 Test Case 2: ดูรายละเอียดข้อสอบ (Read - Detail)

**Steps:**
1. คลิกที่ข้อสอบใดข้อสอบหนึ่ง (เช่น Test ID: 1)
2. URL จะเป็น: http://localhost:3000/tests/1
3. ควรเห็นรายละเอียดข้อสอบเต็ม ๆ

**สิ่งที่ควรเห็น:**
- ✅ ชื่อข้อสอบ (`title`)
- ✅ คำอธิบาย (`description`)
- ✅ ประเภท (`type`)
- ✅ คะแนนรวม (`total_marks`)
- ✅ คะแนนผ่าน (`passing_marks`)
- ✅ เวลาที่ใช้ (`time_limit` นาที)
- ✅ จำนวนครั้งที่สอบได้ (`attempts_allowed`)
- ✅ สถานะ (`status`)
- ✅ ผู้สอน (`instructor_id`)
- ✅ วันที่สร้าง (`created_at`)
- ✅ วันที่แก้ไข (`updated_at`)

**สิ่งที่ต้องตรวจสอบ:**
- ❌ ไม่มี field names แบบเก่า (test_name, test_type, total_score)
- ❌ ไม่มี undefined values
- ❌ Dates แสดงผลถูกต้อง (created_at, updated_at)

---

### 📝 Test Case 3: สร้างข้อสอบใหม่ (Create)

**Steps:**
1. Login ด้วย Admin หรือ Instructor
2. เปิด: http://localhost:3000/tests/create
3. กรอกข้อมูล:
   - **ชื่อข้อสอบ** (Title): "ทดสอบ HTML Interface - Quiz 1"
   - **คำอธิบาย** (Description): "นี่คือข้อสอบทดสอบจากหน้าจอ HTML"
   - **ประเภท** (Type): Quiz
   - **คะแนนรวม** (Total Marks): 100
   - **คะแนนผ่าน** (Passing Marks): 70
   - **เวลา** (Time Limit): 60 นาที
   - **จำนวนครั้ง** (Attempts): 3
   - **สถานะ** (Status): Active
4. คลิก "บันทึก" หรือ "Save"

**สิ่งที่ควรเกิดขึ้น:**
- ✅ บันทึกสำเร็จ
- ✅ แสดงข้อความ "สร้างข้อสอบสำเร็จ" หรือ "Test created successfully"
- ✅ Redirect ไปหน้ารายละเอียดข้อสอบที่สร้าง
- ✅ ข้อสอบใหม่มี `test_id` ที่ auto-increment ถูกต้อง

**สิ่งที่ต้องตรวจสอบ:**
- ❌ ไม่มี Error: "Invalid column name"
- ❌ ไม่มี Error: "Violation of PRIMARY KEY constraint"
- ❌ ไม่มี Error: "Foreign Key constraint"
- ❌ Field mapping ทำงานถูกต้อง (รับทั้ง test_name และ title)

---

### 📝 Test Case 4: แก้ไขข้อสอบ (Update)

**Steps:**
1. เปิดรายละเอียดข้อสอบที่สร้างไว้
2. คลิก "แก้ไข" หรือ "Edit"
3. แก้ไขข้อมูล:
   - เปลี่ยน **Title**: "ทดสอบ HTML Interface - Quiz 1 (Edited)"
   - เปลี่ยน **Passing Marks**: 75
   - เปลี่ยน **Status**: Draft
4. คลิก "บันทึก"

**สิ่งที่ควรเกิดขึ้น:**
- ✅ บันทึกการแก้ไขสำเร็จ
- ✅ แสดงข้อความ "แก้ไขสำเร็จ" หรือ "Updated successfully"
- ✅ ข้อมูลที่แก้ไขแสดงผลถูกต้อง
- ✅ `updated_at` อัพเดทเป็นเวลาปัจจุบัน

**สิ่งที่ต้องตรวจสอบ:**
- ❌ ไม่มี Error เมื่อบันทึก
- ❌ Data ที่แก้ไขถูก save ลง database
- ❌ ไม่มี column name errors

---

### 📝 Test Case 5: ลบข้อสอบ (Soft Delete)

**Steps:**
1. เปิดรายละเอียดข้อสอบที่ต้องการลบ
2. คลิก "ลบ" หรือ "Delete"
3. Confirm การลบ

**สิ่งที่ควรเกิดขึ้น:**
- ✅ ลบสำเร็จ (Soft Delete)
- ✅ `status` เปลี่ยนเป็น "Deleted"
- ✅ ข้อสอบยังอยู่ใน database แต่ไม่แสดงในรายการ
- ✅ แสดงข้อความ "ลบสำเร็จ"

**สิ่งที่ต้องตรวจสอบ:**
- ❌ ไม่ลบข้อมูลจริง ๆ (hard delete)
- ❌ เป็น soft delete (status = 'Deleted')

---

### 📝 Test Case 6: ทดสอบการแสดงผล TestAttempts

**Steps:**
1. เปิดรายละเอียดข้อสอบที่มีผู้ทำข้อสอบแล้ว
2. ดูรายการ "ผู้ที่เคยทำข้อสอบ" หรือ "Test Attempts"

**สิ่งที่ควรเห็น:**
- ✅ รายการคนที่ทำข้อสอบนี้
- ✅ แสดง user_id ของผู้ทำข้อสอบ
- ✅ แสดงคะแนนที่ได้ (score)
- ✅ แสดงสถานะ (status: In Progress, Completed, Abandoned)
- ✅ แสดงวันเวลาที่เริ่มทำ
- ✅ ข้อมูลมาจากตาราง `TestAttempts` (PascalCase)

**สิ่งที่ต้องตรวจสอบ:**
- ❌ ไม่มี Error: "Invalid object name 'test_attempts'"
- ❌ ต้องใช้ชื่อตารางที่ถูกต้อง: `TestAttempts`

---

## 🐛 Errors ที่แก้ไขแล้ว - ควรไม่เห็นอีก

### 1. Schema Mismatch Errors - **แก้แล้ว ✅**
```
❌ ก่อนแก้: Invalid column name 'test_name'
❌ ก่อนแก้: Invalid column name 'test_type'
❌ ก่อนแก้: Invalid column name 'total_score'
❌ ก่อนแก้: Invalid column name 'created_date'

✅ หลังแก้: ใช้ชื่อ column ที่ถูกต้อง (title, type, total_marks, created_at)
```

### 2. Table Name Error - **แก้แล้ว ✅**
```
❌ ก่อนแก้: Invalid object name 'test_attempts'
✅ หลังแก้: ใช้ชื่อตารางที่ถูกต้อง 'TestAttempts' (PascalCase)
```

### 3. Primary Key Error - **แก้แล้ว ✅**
```
❌ ก่อนแก้: Violation of PRIMARY KEY constraint (duplicate test_id)
✅ หลังแก้: ใช้ SCOPE_IDENTITY() สำหรับ auto-increment
✅ IDENTITY column ถูก reseed แล้ว
```

### 4. Foreign Key Error - **แก้แล้ว ✅**
```
❌ ก่อนแก้: Foreign key constraint (instructor_id ไม่มีในระบบ)
✅ หลังแก้: ใช้ instructor_id ที่มีจริง (user_id: 17)
```

---

## 📊 การตรวจสอบข้อมูลใน Database

**ถ้าต้องการตรวจสอบข้อมูลใน database ว่าบันทึกถูกต้อง:**

### ตรวจสอบข้อสอบที่สร้าง:
```javascript
node -e "const { poolPromise } = require('./config/database'); (async () => { const pool = await poolPromise; const result = await pool.request().query('SELECT TOP 5 test_id, title, type, total_marks, passing_marks, status, created_at FROM tests ORDER BY test_id DESC'); console.table(result.recordset); })().catch(console.error)"
```

### ตรวจสอบ TestAttempts:
```javascript
node -e "const { poolPromise } = require('./config/database'); (async () => { const pool = await poolPromise; const result = await pool.request().query('SELECT TOP 5 * FROM TestAttempts ORDER BY started_at DESC'); console.table(result.recordset); })().catch(console.error)"
```

---

## 🎯 สรุป - สิ่งที่ต้องทดสอบผ่าน HTML

### ✅ Checklist การทดสอบ:

- [ ] 1. เปิด server ได้สำเร็จ (http://localhost:3000)
- [ ] 2. Login เข้าสู่ระบบได้
- [ ] 3. เปิดหน้ารายการข้อสอบได้ (http://localhost:3000/tests)
- [ ] 4. แสดงรายการข้อสอบถูกต้อง (ไม่มี undefined/null)
- [ ] 5. เปิดรายละเอียดข้อสอบได้ (http://localhost:3000/tests/:test_id)
- [ ] 6. แสดงข้อมูล field ทั้งหมดถูกต้อง (title, type, total_marks, etc.)
- [ ] 7. สร้างข้อสอบใหม่ได้ (http://localhost:3000/tests/create)
- [ ] 8. บันทึกข้อสอบลง database สำเร็จ
- [ ] 9. test_id auto-increment ถูกต้อง
- [ ] 10. แก้ไขข้อสอบได้
- [ ] 11. บันทึกการแก้ไขสำเร็จ
- [ ] 12. updated_at อัพเดทอัตโนมัติ
- [ ] 13. ลบข้อสอบได้ (soft delete)
- [ ] 14. status เปลี่ยนเป็น 'Deleted'
- [ ] 15. แสดง TestAttempts ได้ถูกต้อง
- [ ] 16. ไม่มี console errors
- [ ] 17. ไม่มี SQL errors
- [ ] 18. Field mapping ทำงานถูกต้อง (รองรับทั้ง old และ new field names)

---

## 🔍 การตรวจสอบ Errors ในเบราว์เซอร์

### เปิด Developer Console:
- **Chrome/Edge:** กด `F12` หรือ `Ctrl+Shift+I`
- **Firefox:** กด `F12` หรือ `Ctrl+Shift+K`

### สิ่งที่ต้องดูใน Console:
- ❌ ไม่มี JavaScript errors
- ❌ ไม่มี Network errors (500, 400, 404)
- ❌ ไม่มี SQL error messages
- ✅ API calls ส่งกลับ status 200 (สำเร็จ)
- ✅ Response data ถูกต้อง

---

## 📝 การบันทึกผลการทดสอบ

**เมื่อทดสอบเสร็จ ให้บันทึก:**

1. **Test Cases ที่ผ่าน** - เช่น "สร้างข้อสอบได้", "แก้ไขข้อสอบได้"
2. **Test Cases ที่ไม่ผ่าน** - บันทึก error message ที่เจอ
3. **Screenshots** - ถ่ายหน้าจอหลักฐาน
4. **Browser Console Errors** - copy error messages

---

## 🆘 ถ้าเจอปัญหา

### ปัญหาที่อาจพบ:

**1. Server ไม่เปิด:**
```bash
# ตรวจสอบว่า port 3000 ว่างหรือไม่
netstat -ano | findstr :3000

# ถ้า port ติด ให้ kill process
taskkill /PID <PID> /F
```

**2. Database ไม่ connect:**
```bash
# ทดสอบ connection
node test-db-connection.js
```

**3. Login ไม่ได้:**
- ตรวจสอบว่ามี user ในระบบ
- ตรวจสอบ password ถูกต้อง
- ดู console errors

**4. หน้าจอแสดง Error:**
- เปิด Developer Console ดู error
- ตรวจสอบ Network tab ว่า API call fail หรือไม่
- บันทึก error message

---

## 📞 Support Files

**ไฟล์สำคัญที่เกี่ยวข้อง:**
- `models/Test.js` - Model ที่แก้ไขแล้ว
- `controllers/testController.js` - Controller ที่แก้ไขแล้ว
- `routes/testRoutes.js` - Routes สำหรับ Tests
- `views/tests/` - หน้าจอ HTML/EJS ทั้งหมด
- `FIX_SUMMARY.md` - สรุปการแก้ไข Model/Controller
- `ERROR_REPORT.md` - รายงาน errors ที่พบและแก้ไข

---

## ✅ เมื่อทดสอบเสร็จแล้ว

**ถ้าทุกอย่างทำงานถูกต้อง:**
- ✅ Model และ Controller ทำงานถูกต้อง 100%
- ✅ ระบบ Tests พร้อมใช้งานจริง
- ✅ สามารถ deploy ได้

**ถ้ายังมีปัญหา:**
- บันทึก error messages
- บันทึก test cases ที่ fail
- แจ้งให้ทีมแก้ไขเพิ่มเติม

---

**จัดทำโดย:** Error Detection & Fix System
**วันที่:** 23 พฤศจิกายน 2025
**สถานะ:** ✅ **พร้อมทดสอบผ่าน HTML Interface**
