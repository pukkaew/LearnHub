# 🧪 คู่มือทดสอบระบบสร้างคอร์สบนหน้าเว็บจริง

**URL ทดสอบ:** http://localhost:3000/courses/create

---

## 📋 เตรียมความพร้อม

### ✅ Checklist ก่อนเริ่ม:
- [ ] Server รันอยู่ที่ port 3000
- [ ] Login เข้าระบบด้วย Admin/Instructor
- [ ] เปิด Browser DevTools (F12)
- [ ] เปิด Console tab
- [ ] เปิด Network tab
- [ ] Disable cache (☑️ Disable cache ใน Network tab)

---

## 🎯 Test Case 1: ข้อมูลครบถ้วน (Happy Path)

### วัตถุประสงค์: ทดสอบว่าสร้างคอร์สสำเร็จเมื่อกรอกข้อมูลครบถ้วน

### ขั้นตอน:

#### Step 1: ข้อมูลพื้นฐาน
1. ชื่อหลักสูตร: `"Test Course 2025 - Complete System"`
2. รหัสหลักสูตร: (auto-generate)
3. หมวดหมู่: เลือกอันแรก
4. ระดับความยาก: `Beginner`
5. ประเภทหลักสูตร: `mandatory`
6. ภาษา: `th`
7. กด "ถัดไป"

#### Step 2: รายละเอียดหลักสูตร
1. คำอธิบาย: กรอก **อย่างน้อย 50 ตัวอักษร**
   ```
   หลักสูตรนี้มีวัตถุประสงค์เพื่อทดสอบระบบการสร้างคอร์สอย่างครบถ้วน ครอบคลุมทุกฟีเจอร์
   ```
2. วัตถุประสงค์ (3 ข้อ):
   - `"เพื่อทดสอบระบบการสร้างคอร์ส"`
   - `"เพื่อตรวจสอบ validation"`
   - `"เพื่อทดสอบ data transformation"`
3. ระยะเวลา: `2` ชม. `30` นาที
4. หน่วยงาน: เลือก 1-2 แห่ง
5. ตำแหน่ง: เลือก 1-2 ตำแหน่ง
6. กด "ถัดไป"

#### Step 3: เนื้อหาและสื่อการสอน
1. รูปหน้าปก: ไม่อัพโหลด (ทดสอบ default image)
2. วิดีโอแนะนำ: ข้าม
3. บทเรียนที่ 1:
   - ชื่อ: `"บทนำ - ทดสอบระบบ"`
   - ระยะเวลา: `30` นาที
   - คำอธิบาย: `"บทเรียนทดสอบระบบ"`
   - ☑️ เพิ่มแบบทดสอบย่อย:
     - จำนวนข้อ: `5`
     - เวลา: `10` นาที
     - คะแนนผ่าน: `60`%
     - ทำได้: `3` ครั้ง
     - ☑️ ต้องทำก่อนไปบทถัดไป
     - ☑️ นับคะแนนรวม
4. กด "เพิ่มบทเรียน" → เพิ่มบทที่ 2:
   - ชื่อ: `"บทที่ 2 - ขั้นสูง"`
   - ระยะเวลา: `45` นาที
   - (ไม่มีแบบทดสอบย่อย)
5. กด "ถัดไป"

#### Step 4: ข้อสอบและการประเมินผล
1. เลือก "สร้างข้อสอบใหม่"
2. ประเภท: `Final Assessment` (แบบทดสอบท้ายหลักสูตร)
3. ชื่อข้อสอบ: `"แบบทดสอบท้ายหลักสูตร"`
4. คำอธิบาย: `"ทดสอบความรู้หลังเรียนจบ"`
5. การตั้งค่าข้อสอบ:
   - เกณฑ์ผ่าน: `70`%
   - จำนวนครั้ง: `2`
   - เวลาทำ: `60` นาที
6. วันเวลาเปิดข้อสอบ:
   - เปิดให้ทำตั้งแต่: `พรุ่งนี้ 09:00`
   - ปิดข้อสอบเมื่อ: `3 วันข้างหน้า 17:00`
7. พฤติกรรมข้อสอบ:
   - ☑️ นับคะแนน
   - ☑️ ต้องทำ
   - ☑️ ต้องผ่าน
   - แสดงเฉลย: `after_test`
8. กด "สร้างหลักสูตร"

### ผลที่คาดหวัง:
1. ✅ ไม่มี validation error
2. ✅ แสดง loading spinner
3. ✅ Console log: `🔍 FINAL DATA TO SEND:`
4. ✅ Server response: `{ success: true }`
5. ✅ Redirect ไป course detail page
6. ✅ แสดง success message

### เช็คใน Console:
```javascript
// ควรเห็น log นี้:
🔍 FINAL DATA TO SEND:
  passing_score: 70
  max_attempts: 2
  max_students: null
  target_departments: [id, ...]
  target_positions: [id, ...]
```

### เช็คใน Network Tab:
1. Request: `POST /courses/api/create`
2. Status: `200 OK` หรือ `201 Created`
3. Response body: `{ success: true, data: {...} }`

---

## 🎯 Test Case 2: Validation Errors (Error Cases)

### Test 2.1: ชื่อหลักสูตรสั้นเกินไป

**ขั้นตอน:**
1. Step 1 → ชื่อหลักสูตร: `"Test"` (4 ตัวอักษร)
2. กด "ถัดไป"

**ผลที่คาดหวัง:**
- ❌ แสดง error: "กรุณากรอกชื่อหลักสูตรอย่างน้อย 5 ตัวอักษร"
- ❌ ไม่ให้ไปหน้าถัดไป

---

### Test 2.2: คำอธิบายสั้นเกินไป

**ขั้นตอน:**
1. Step 1 → กรอกถูกต้อง → "ถัดไป"
2. Step 2 → คำอธิบาย: `"สั้นมาก"` (8 ตัวอักษร)
3. กด "ถัดไป"

**ผลที่คาดหวัง:**
- ❌ แสดง error: "กรุณากรอกคำอธิบายหลักสูตรอย่างน้อย 50 ตัวอักษร"
- ❌ ไม่ให้ไปหน้าถัดไป

---

### Test 2.3: วัตถุประสงค์น้อยเกินไป

**ขั้นตอน:**
1. Step 2 → คำอธิบายถูกต้อง
2. วัตถุประสงค์: กรอกเพียง 2 ข้อ
3. กด "ถัดไป"

**ผลที่คาดหวัง:**
- ❌ แสดง error: "กรุณากรอกวัตถุประสงค์การเรียนรู้อย่างน้อย 3 ข้อ"

---

### Test 2.4: ระยะเวลาน้อยเกินไป

**ขั้นตอน:**
1. Step 2 → ระยะเวลา: `0` ชม. `30` นาที
2. กด "ถัดไป"

**ผลที่คาดหวัง:**
- ❌ แสดง error: "กรุณาระบุระยะเวลาเรียนอย่างน้อย 1 ชั่วโมง หรือมากกว่า"

---

### Test 2.5: ไม่มีบทเรียน

**ขั้นตอน:**
1. Step 3 → ลบบทเรียนทั้งหมด
2. กด "ถัดไป"

**ผลที่คาดหวัง:**
- ❌ แสดง error: "กรุณาเพิ่มบทเรียนอย่างน้อย 1 บทเรียน"

---

### Test 2.6: ไม่เลือกประเภทข้อสอบ

**ขั้นตอน:**
1. Step 4 → ไม่เลือก assessment_type
2. กด "สร้างหลักสูตร"

**ผลที่คาดหวัง:**
- ❌ แสดง error

---

## 🎯 Test Case 3: Data Transformation

### Test 3.1: ระยะเวลา (duration calculation)

**ทดสอบ:**
- Input: `2` ชม. `45` นาที
- Expected output: `2.75` hours

**วิธีเช็ค:**
```javascript
// ใน Console log:
duration_hours: 2.75
```

---

### Test 3.2: Empty max_students

**ทดสอบ:**
- ไม่กรอก "จำนวนผู้เรียนสูงสุด"
- Expected output: `null` (ไม่ใช่ `undefined`)

**วิธีเช็ค:**
```javascript
// ใน Console log:
max_students: null  // ✅ ถูกต้อง
// NOT: max_students: undefined  // ❌ ผิด
```

---

### Test 3.3: Multi-select departments

**ทดสอบ:**
- เลือกหน่วยงาน 2 แห่ง
- Expected output: `[id1, id2]`

**วิธีเช็ค:**
```javascript
// ใน Console log:
target_departments: ['48', '49']  // ✅ array
// NOT: target_departments: undefined  // ❌ ผิด
```

---

## 🎯 Test Case 4: Special Features

### Test 4.1: Test Type - มีวันเวลา vs ไม่มี

**ทดสอบ A: Final Assessment (ต้องมีวันเวลา)**
1. เลือก "Final Assessment"
2. **เช็ค:** ช่อง "ช่วงเวลาเปิดข้อสอบ" ต้อง **แสดง**
3. ถ้าไม่กรอกวันเวลา → ควร error

**ทดสอบ B: Practice Exercise (ไม่ต้องมีวันเวลา)**
1. เลือก "Practice Exercise"
2. **เช็ค:** ช่อง "ช่วงเวลาเปิดข้อสอบ" ต้อง **ซ่อน**
3. ไม่กรอกวันเวลา → ควรผ่าน

---

### Test 4.2: Lesson Quiz (Knowledge Check)

**ทดสอบ:**
1. เพิ่มบทเรียน
2. ☑️ เปิด "เพิ่มแบบทดสอบย่อยท้ายบทเรียน"
3. **เช็ค:** ฟอร์มตั้งค่า quiz ต้อง**แสดง**
4. กรอกข้อมูล quiz
5. Submit course

**ผลที่คาดหวัง:**
- ✅ ข้อมูล quiz ถูกส่งไปด้วย
- ✅ บันทึกลง database

---

## 📊 Checklist สรุป

### ✅ Functional Tests:
- [ ] TC1: สร้างคอร์สสำเร็จ (ข้อมูลครบ)
- [ ] TC2.1: ชื่อสั้นเกินไป → error
- [ ] TC2.2: คำอธิบายสั้นเกินไป → error
- [ ] TC2.3: วัตถุประสงค์น้อย → error
- [ ] TC2.4: ระยะเวลาน้อย → error
- [ ] TC2.5: ไม่มีบทเรียน → error
- [ ] TC2.6: ไม่เลือกข้อสอบ → error

### ✅ Data Transformation:
- [ ] TC3.1: Duration calculation (2.75 hours)
- [ ] TC3.2: Empty max_students → null
- [ ] TC3.3: Multi-select → array

### ✅ Special Features:
- [ ] TC4.1: Test type วันเวลา (แสดง/ซ่อน)
- [ ] TC4.2: Lesson quiz (เพิ่มได้)

---

## 🐛 บันทึกปัญหา (ถ้าพบ)

### ปัญหาที่ 1:
- **อาการ:** [อธิบาย]
- **ขั้นตอนเกิด:** [...]
- **Error message:** [...]
- **Screenshot/Console log:** [...]

### ปัญหาที่ 2:
...

---

## ✅ ผลการทดสอบ

**ผู้ทดสอบ:** _____________
**วันที่:** _____________
**เวลา:** _____________

**สรุป:**
- ✅ ผ่าน: ___ / 13 test cases
- ❌ ไม่ผ่าน: ___ / 13 test cases

**หมายเหตุ:** _______________________________________________

---

## 🎯 Quick Test (ทดสอบรวดเร็ว)

หากต้องการทดสอบอย่างรวดเร็ว ให้ทำตามนี้:

1. **เปิด Console** (F12)
2. **Paste script นี้:**

```javascript
console.clear();
console.log('🧪 Starting Quick Test...\n');

// Test 1: Check page loaded
console.log('✅ Test 1: Page loaded');

// Test 2: Check form exists
const form = document.getElementById('create-course-form');
console.log(form ? '✅ Test 2: Form exists' : '❌ Test 2: Form NOT found');

// Test 3: Check multi-select fields
const depts = document.getElementById('target_departments');
const pos = document.getElementById('target_positions');
console.log(depts && pos ? '✅ Test 3: Multi-select fields exist' : '❌ Test 3: Multi-select fields NOT found');

// Test 4: Check translations loaded
const hasTrans = window.testTypeTranslations ? true : false;
console.log(hasTrans ? '✅ Test 4: Translations loaded' : '❌ Test 4: Translations NOT loaded');

// Test 5: Check Flatpickr loaded
const hasFlatpickr = typeof flatpickr !== 'undefined';
console.log(hasFlatpickr ? '✅ Test 5: Flatpickr loaded' : '❌ Test 5: Flatpickr NOT loaded');

console.log('\n🎉 Quick Test Complete!');
```

3. ดูผล → ควรได้ ✅ ทั้งหมด

---

**สำเร็จ!** หากผ่านทุก test → ระบบพร้อมใช้งาน 🚀
