# แก้ไข: ตำแหน่งเป้าหมาย และ แผนกเป้าหมาย เป็น Optional

**วันที่:** 17 พฤศจิกายน 2025
**ปัญหา:** ฟิลด์ target_positions และ target_departments เป็น required แต่ไม่มีข้อมูลในฐานข้อมูล

---

## 🔍 สาเหตุปัญหา

1. หน้า **create** มีฟิลด์ target_positions และ target_departments แบบ **required**
2. แต่ข้อมูลที่บันทึกลง database เป็น **empty object** `{}`
3. หน้า **edit** พยายามแสดงข้อมูลแต่ไม่มีอะไร
4. ผู้ใช้สับสนว่าทำไมมีฟิลด์แต่ไม่มีข้อมูล

---

## ✅ การแก้ไข

### 1. หน้า Create - เอา required ออก

**ไฟล์:** `views/courses/create.ejs` (Line 226-246)

**เดิม:**
```html
<label>ตำแหน่งเป้าหมาย <span class="text-red-500">*</span></label>
<select id="target_positions" name="target_positions[]" multiple required>
    ...
</select>
<p>กด Ctrl เพื่อเลือกหลายตำแหน่ง</p>
```

**ใหม่:**
```html
<label>ตำแหน่งเป้าหมาย <span class="text-gray-400 text-xs">(ไม่บังคับ)</span></label>
<select id="target_positions" name="target_positions[]" multiple>
    ...
</select>
<p>กด Ctrl เพื่อเลือกหลายตำแหน่ง (หรือไม่เลือกก็ได้)</p>
```

**การเปลี่ยนแปลง:**
- ✅ เอา `required` attribute ออก
- ✅ เปลี่ยน `*` (สีแดง) เป็น `(ไม่บังคับ)` (สีเทา)
- ✅ เพิ่มคำอธิบาย "หรือไม่เลือกก็ได้"

---

### 2. หน้า Edit - เพิ่มคำอธิบาย optional

**ไฟล์:** `views/courses/edit.ejs` (Line 149-163)

**เดิม:**
```html
<label>ตำแหน่งเป้าหมาย</label>
<select id="target_positions" name="target_positions[]" multiple size="5">
    ...
</select>
<p>กด Ctrl (Cmd) เพื่อเลือกหลายตำแหน่ง</p>
```

**ใหม่:**
```html
<label>ตำแหน่งเป้าหมาย <span class="text-gray-400 text-xs">(ไม่บังคับ)</span></label>
<select id="target_positions" name="target_positions[]" multiple size="5">
    ...
</select>
<p>กด Ctrl (Cmd) เพื่อเลือกหลายตำแหน่ง (หรือไม่เลือกก็ได้)</p>
```

**การเปลี่ยนแปลง:**
- ✅ เพิ่ม `(ไม่บังคับ)` ใน label
- ✅ เพิ่มคำอธิบาย "หรือไม่เลือกก็ได้"

---

### 3. populateForm() - จัดการ empty object

**ไฟล์:** `views/courses/edit.ejs` (Line 475-496)

**เดิม:**
```javascript
if (course.target_audience) {
    const targetAudience = typeof course.target_audience === 'string'
        ? JSON.parse(course.target_audience)
        : course.target_audience;

    if (targetAudience.positions && Array.isArray(targetAudience.positions)) {
        // Select positions
    }
}
```

**ใหม่:**
```javascript
// Set target audience (optional - may be empty)
if (course.target_audience && typeof course.target_audience === 'object') {
    const targetAudience = typeof course.target_audience === 'string'
        ? JSON.parse(course.target_audience)
        : course.target_audience;

    // Check if target_audience has data (not empty object)
    if (targetAudience.positions && Array.isArray(targetAudience.positions) && targetAudience.positions.length > 0) {
        // Select positions
    }

    if (targetAudience.departments && Array.isArray(targetAudience.departments) && targetAudience.departments.length > 0) {
        // Select departments
    }
}
// If no target_audience data, leave selects empty (which is fine - optional field)
```

**การเปลี่ยนแปลง:**
- ✅ เพิ่มการเช็ค `length > 0` เพื่อไม่ให้ error กับ empty array
- ✅ เพิ่ม comment อธิบายว่าไม่มีข้อมูลก็ไม่เป็นไร
- ✅ ถ้าไม่มีข้อมูล dropdown จะว่างเปล่า (ไม่มีอะไรถูกเลือก)

---

### 4. Model.update() - รองรับ empty arrays

**ไฟล์:** `models/Course.js` (Line 359-374)

**เดิม:**
```javascript
if (updateData.target_positions !== undefined || updateData.target_departments !== undefined) {
    const targetAudienceJson = JSON.stringify({
        positions: updateData.target_positions || [],
        departments: updateData.target_departments || []
    });
    updateFields.push('target_audience = @targetAudience');
    request.input('targetAudience', sql.NVarChar(sql.MAX), targetAudienceJson);
}
```

**ใหม่:**
```javascript
if (updateData.target_positions !== undefined || updateData.target_departments !== undefined) {
    const positions = Array.isArray(updateData.target_positions) && updateData.target_positions.length > 0
        ? updateData.target_positions
        : [];
    const departments = Array.isArray(updateData.target_departments) && updateData.target_departments.length > 0
        ? updateData.target_departments
        : [];

    // If both are empty, store null instead of empty object
    const targetAudienceJson = (positions.length > 0 || departments.length > 0)
        ? JSON.stringify({ positions, departments })
        : null;

    updateFields.push('target_audience = @targetAudience');
    request.input('targetAudience', sql.NVarChar(sql.MAX), targetAudienceJson);
}
```

**การเปลี่ยนแปลง:**
- ✅ เช็คว่า array มีข้อมูลจริงหรือไม่
- ✅ ถ้าทั้ง 2 ตัวว่าง → บันทึกเป็น `null` แทนที่จะเป็น `{}`
- ✅ ป้องกันไม่ให้มี empty object ในฐานข้อมูล

---

## 📊 ผลลัพธ์

### ก่อนแก้ไข ❌

**หน้า Create:**
```
ตำแหน่งเป้าหมาย *          ← required, บังคับต้องเลือก
[dropdown]

แผนกเป้าหมาย *             ← required, บังคับต้องเลือก
[dropdown]
```

**Database:**
```json
{
  "target_audience": {}     ← Empty object
}
```

**หน้า Edit:**
```
ตำแหน่งเป้าหมาย
[dropdown]                  ← ว่างเปล่า (ไม่มีอะไรเลือก)
กด Ctrl เพื่อเลือกหลายตำแหน่ง

แผนกเป้าหมาย
[dropdown]                  ← ว่างเปล่า
กด Ctrl เพื่อเลือกหลายแผนก
```

❌ **ปัญหา:** ผู้ใช้สับสนว่าทำไมมีฟิลด์แต่ไม่มีข้อมูล

---

### หลังแก้ไข ✅

**หน้า Create:**
```
ตำแหน่งเป้าหมาย (ไม่บังคับ)    ← ชัดเจนว่าไม่บังคับ
[dropdown]
กด Ctrl เพื่อเลือกหลายตำแหน่ง (หรือไม่เลือกก็ได้)

แผนกเป้าหมาย (ไม่บังคับ)       ← ชัดเจนว่าไม่บังคับ
[dropdown]
กด Ctrl เพื่อเลือกหลายแผนก (หรือไม่เลือกก็ได้)
```

**Database (ถ้าไม่เลือก):**
```json
{
  "target_audience": null     ← null แทน empty object
}
```

**Database (ถ้าเลือก):**
```json
{
  "target_audience": {
    "positions": ["1", "2"],
    "departments": ["1", "2", "3"]
  }
}
```

**หน้า Edit:**
```
ตำแหน่งเป้าหมาย (ไม่บังคับ)    ← ชัดเจนว่าไม่บังคับ
[dropdown]
กด Ctrl (Cmd) เพื่อเลือกหลายตำแหน่ง (หรือไม่เลือกก็ได้)

แผนกเป้าหมาย (ไม่บังคับ)       ← ชัดเจนว่าไม่บังคับ
[dropdown]
กด Ctrl (Cmd) เพื่อเลือกหลายแผนก (หรือไม่เลือกก็ได้)
```

✅ **แก้แล้ว:** ผู้ใช้เข้าใจว่าฟิลด์นี้ไม่บังคับ สามารถเลือกหรือไม่เลือกก็ได้

---

## 🧪 การทดสอบ

### Test Case 1: สร้างคอร์สโดยไม่เลือกตำแหน่ง/แผนก

```bash
# 1. เข้าหน้าสร้างคอร์ส
http://localhost:3000/courses/create

# 2. กรอกข้อมูลทั้งหมด แต่ไม่เลือก target_positions และ target_departments
# 3. คลิกสร้างหลักสูตร
```

**ผลที่คาดหวัง:**
- ✅ สร้างสำเร็จ (ไม่ error)
- ✅ Database: `target_audience = null` หรือ `{}`

---

### Test Case 2: สร้างคอร์สโดยเลือกตำแหน่ง/แผนก

```bash
# 1. เข้าหน้าสร้างคอร์ส
# 2. กรอกข้อมูลทั้งหมด
# 3. เลือก target_positions: ผู้จัดการ, พนักงานทั่วไป
# 4. เลือก target_departments: IT, HR, Sales
# 5. คลิกสร้างหลักสูตร
```

**ผลที่คาดหวัง:**
- ✅ สร้างสำเร็จ
- ✅ Database: `target_audience = {"positions":["manager","staff"],"departments":["IT","HR","Sales"]}`

---

### Test Case 3: แก้ไขคอร์สที่ไม่มีตำแหน่ง/แผนก

```bash
# 1. เข้าหน้าแก้ไข course ที่มี target_audience = null หรือ {}
http://localhost:3000/courses/1/edit

# 2. ตรวจสอบ dropdown
```

**ผลที่คาดหวัง:**
- ✅ ไม่มี error
- ✅ Dropdown ว่างเปล่า (ไม่มีอะไรถูกเลือก)
- ✅ มีข้อความ "(ไม่บังคับ)"

---

### Test Case 4: แก้ไขคอร์สที่มีตำแหน่ง/แผนก

```bash
# 1. เข้าหน้าแก้ไข course ที่มี target_audience ข้อมูลอยู่
http://localhost:3000/courses/1/edit

# 2. ตรวจสอบ dropdown
```

**ผลที่คาดหวัง:**
- ✅ ไม่มี error
- ✅ Dropdown แสดงตัวเลือกที่เคยบันทึกไว้
- ✅ สามารถเลือกเพิ่มหรือยกเลิกการเลือกได้

---

### Test Case 5: บันทึกแก้ไขโดยไม่เลือกตำแหน่ง/แผนก

```bash
# 1. เข้าหน้าแก้ไข
# 2. ไม่เลือกอะไรใน target_positions และ target_departments (เคลียร์ทิ้งหมด)
# 3. คลิกบันทึกการแก้ไข
# 4. Refresh หน้า
```

**ผลที่คาดหวัง:**
- ✅ บันทึกสำเร็จ
- ✅ Database: `target_audience = null`
- ✅ Refresh แล้ว dropdown ยังคงว่าง

---

## 📋 สรุป

### การเปลี่ยนแปลง:

1. ✅ **หน้า Create** - เอา `required` ออก + เพิ่ม "(ไม่บังคับ)"
2. ✅ **หน้า Edit** - เพิ่ม "(ไม่บังคับ)" + คำอธิบายชัดเจน
3. ✅ **populateForm()** - จัดการ empty object ให้ถูกต้อง
4. ✅ **Model.update()** - บันทึก `null` แทน `{}` ถ้าไม่มีข้อมูล

### ผลลัพธ์:

- ✅ ไม่มี error จาก empty object
- ✅ ผู้ใช้เข้าใจว่าฟิลด์ไม่บังคับ
- ✅ สามารถเลือกหรือไม่เลือกก็ได้
- ✅ Database สะอาด (null หรือมีข้อมูลจริง ไม่มี empty object)

### ไฟล์ที่แก้ไข:

1. ✅ `views/courses/create.ejs` (Line 226-246)
2. ✅ `views/courses/edit.ejs` (Line 149-163, 475-496)
3. ✅ `models/Course.js` (Line 359-374)

---

**สถานะ:** ✅ **เสร็จสมบูรณ์**
**ทดสอบ:** พร้อมใช้งาน - เข้าหน้าสร้างและแก้ไขคอร์ส ตรวจสอบว่า "(ไม่บังคับ)" แสดงถูกต้อง

**Updated:** 17 พฤศจิกายน 2025
