<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Sarabun', 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #1e3a5f 0%, #0f1f33 100%);
            min-height: 100vh;
        }
        .result-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }
        .success-icon {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            box-shadow: 0 0 40px rgba(16, 185, 129, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
        }
        @keyframes checkmark {
            0% { transform: scale(0); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        .animate-check {
            animation: checkmark 0.5s ease-out forwards;
        }

        /* RESPONSIVE STYLES FOR LARGE SCREENS */
        @media (min-width: 1024px) {
            .result-card { max-width: 32rem; }
        }
        @media (min-width: 1440px) {
            body { font-size: 1.05rem; }
            .result-card { max-width: 36rem; padding: 3rem; }
            .success-icon { width: 120px; height: 120px; }
            h1 { font-size: 1.75rem; }
        }
        @media (min-width: 1920px) {
            body { font-size: 1.1rem; }
            .result-card { max-width: 40rem; padding: 3.5rem; }
            .success-icon { width: 140px; height: 140px; }
            h1 { font-size: 2rem; }
        }
    </style>
</head>
<body class="flex items-center justify-center p-4">
    <div class="result-card max-w-md w-full p-8 text-center">
        <!-- Success Icon -->
        <div class="mb-6">
            <div class="success-icon">
                <i class="fas fa-check text-4xl text-white animate-check"></i>
            </div>
        </div>

        <!-- Header -->
        <div class="mb-6">
            <h1 class="text-2xl font-bold text-gray-800">ทำแบบทดสอบเสร็จสิ้น</h1>
        </div>

        <!-- Back to Dashboard Button -->
        <a href="/applicants/dashboard/<%= test_code %>"
           class="block w-full py-3 px-6 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-semibold">
            <i class="fas fa-arrow-left mr-2"></i>
            กลับหน้ารายการข้อสอบ
        </a>
    </div>

    <script>
        // Auto redirect after 2 seconds
        setTimeout(() => {
            window.location.href = '/applicants/dashboard/<%= test_code %>';
        }, 2000);
    </script>
</body>
</html>
