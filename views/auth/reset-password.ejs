<!DOCTYPE html>
<html lang="<%= typeof currentLanguage !== 'undefined' ? currentLanguage : 'th' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%
        const siteName = typeof getSetting === 'function' ? getSetting('site_name', 'Rukchai Hongyen LearnHub') : 'Rukchai Hongyen LearnHub';
        const logoUrl = typeof getSetting === 'function' ? getSetting('logo_url', '/images/logo.png') : '/images/logo.png';
        const primaryColor = typeof getSetting === 'function' ? getSetting('primary_color', '#0090D3') : '#0090D3';
        const currentLang = typeof currentLanguage !== 'undefined' ? currentLanguage : 'th';
    %>
    <title><%= currentLang === 'en' ? 'Reset Password' : 'รีเซ็ตรหัสผ่าน' %> - <%= siteName %></title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Thai:wght@300;400;500;600;700&display=swap');

        :root {
            --primary-color: <%= primaryColor %>;
            --primary-color-dark: color-mix(in srgb, <%= primaryColor %> 80%, #000 20%);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Noto Sans Thai', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }

        .page-wrapper {
            min-height: 100vh;
            display: flex;
            align-items: stretch;
        }

        .branding-section {
            display: none;
            flex: 1;
            background: linear-gradient(135deg, rgba(0,0,0,0.3) 0%, rgba(0,0,0,0.1) 100%);
            padding: 3rem;
            color: white;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .branding-logo {
            width: 120px;
            height: 120px;
            background: white;
            border-radius: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 2rem;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        }

        .branding-logo img {
            width: 80%;
            height: 80%;
            object-fit: contain;
        }

        .branding-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 0 2px 10px rgba(0,0,0,0.2);
        }

        .branding-subtitle {
            font-size: 1.25rem;
            opacity: 0.9;
            margin-bottom: 3rem;
            max-width: 400px;
        }

        .branding-features {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            text-align: left;
        }

        .branding-feature {
            display: flex;
            align-items: center;
            gap: 1rem;
            font-size: 1rem;
            opacity: 0.95;
        }

        .branding-feature i {
            width: 40px;
            height: 40px;
            background: rgba(255,255,255,0.2);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.1rem;
        }

        .form-section {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }

        .container {
            width: 100%;
            max-width: 440px;
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            position: relative;
            animation: slideUp 0.5s ease-out;
        }

        .language-selector {
            position: absolute;
            top: 1rem;
            right: 1rem;
            z-index: 10;
        }

        .auth-language-button {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 14px;
            border-radius: 10px;
            background: color-mix(in srgb, var(--primary-color) 10%, transparent);
            border: 1px solid color-mix(in srgb, var(--primary-color) 20%, transparent);
            color: var(--primary-color);
            transition: all 0.25s ease;
            cursor: pointer;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .auth-language-button:hover {
            background: color-mix(in srgb, var(--primary-color) 15%, transparent);
        }

        .auth-language-dropdown {
            position: absolute;
            top: calc(100% + 8px);
            right: 0;
            min-width: 140px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
            padding: 6px;
            z-index: 1000;
            opacity: 0;
            visibility: hidden;
            transform: translateY(-10px);
            transition: all 0.25s ease;
        }

        .auth-language-dropdown.open {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .auth-language-option {
            display: block;
            padding: 10px 14px;
            border-radius: 6px;
            color: #374151;
            font-size: 0.8rem;
            font-weight: 500;
            transition: all 0.2s ease;
            cursor: pointer;
            border: none;
            background: none;
            width: 100%;
            text-align: left;
        }

        .auth-language-option:hover {
            background: #f8fafc;
            color: var(--primary-color);
        }

        .auth-language-option.active {
            background: color-mix(in srgb, var(--primary-color) 10%, transparent);
            color: var(--primary-color);
        }

        .header {
            text-align: center;
            margin-bottom: 1.5rem;
            margin-top: 2rem;
        }

        .logo {
            width: 100%;
            max-width: 180px;
            margin: 0 auto 1rem;
        }

        .logo img {
            width: 100%;
            height: auto;
        }

        .title {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .subtitle {
            color: #6b7280;
            font-size: 0.95rem;
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .label {
            display: block;
            font-size: 0.875rem;
            font-weight: 600;
            color: #374151;
            margin-bottom: 0.5rem;
        }

        .input-group {
            position: relative;
        }

        .input {
            width: 100%;
            padding: 0.875rem 1rem 0.875rem 3rem;
            border: 2px solid #e5e7eb;
            border-radius: 0.75rem;
            font-size: 1rem;
            transition: border-color 0.2s;
        }

        .input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px color-mix(in srgb, var(--primary-color) 10%, transparent);
        }

        .input-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: #9ca3af;
        }

        .hint {
            font-size: 0.75rem;
            color: #9ca3af;
            margin-top: 0.25rem;
        }

        .requirements-box {
            background: #f8fafc;
            border-radius: 0.75rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .requirements-box h4 {
            font-size: 0.875rem;
            font-weight: 600;
            color: #374151;
            margin-bottom: 0.75rem;
        }

        .requirement-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.8rem;
            color: #6b7280;
            padding: 0.25rem 0;
        }

        .requirement-item i {
            width: 16px;
            text-align: center;
        }

        .requirement-item.valid {
            color: #16a34a;
        }

        .requirement-item.valid i {
            color: #16a34a;
        }

        .requirement-item.invalid i {
            color: #dc2626;
        }

        .submit-btn {
            width: 100%;
            background: var(--primary-color);
            color: white;
            border: none;
            border-radius: 0.75rem;
            padding: 1rem;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .submit-btn:hover:not(:disabled) {
            background: var(--primary-color-dark);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px color-mix(in srgb, var(--primary-color) 40%, transparent);
        }

        .submit-btn:disabled {
            background: #9ca3af;
            cursor: not-allowed;
            transform: none;
        }

        .links {
            text-align: center;
            margin-top: 1.5rem;
            font-size: 0.9rem;
        }

        .links a {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 500;
        }

        .links a:hover {
            text-decoration: underline;
        }

        .alert {
            border-radius: 0.75rem;
            padding: 1rem;
            margin-bottom: 1rem;
            font-size: 0.875rem;
            display: none;
            align-items: center;
            gap: 0.5rem;
        }

        .alert.show { display: flex; }

        .alert-error {
            background: #fef2f2;
            border: 1px solid #fecaca;
            color: #dc2626;
        }

        .alert-warning {
            background: #fef3c7;
            border: 1px solid #fcd34d;
            color: #92400e;
        }

        .loading-spinner {
            width: 1rem;
            height: 1rem;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-top-color: white;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin { to { transform: rotate(360deg); } }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (min-width: 1024px) {
            .branding-section { display: flex; }
            .form-section {
                background: rgba(255,255,255,0.1);
                backdrop-filter: blur(10px);
            }
            .container { max-width: 480px; }
        }

        @media (min-width: 1440px) {
            .branding-section { padding: 4rem; }
            .branding-logo { width: 150px; height: 150px; }
            .branding-title { font-size: 3rem; }
            .container { max-width: 500px; padding: 2.5rem; }
        }

        @media (min-width: 1920px) {
            .branding-section { padding: 5rem; }
            .branding-logo { width: 180px; height: 180px; border-radius: 32px; }
            .branding-title { font-size: 3.5rem; }
            .branding-subtitle { font-size: 1.5rem; }
            .container { max-width: 540px; padding: 3rem; }
            .title { font-size: 2rem; }
        }

        @media (max-width: 640px) {
            .form-section { padding: 1rem 0.75rem; }
            .container { padding: 1.5rem; border-radius: 12px; }
            .title { font-size: 1.5rem; }
        }
    </style>
</head>
<body>
    <div class="page-wrapper">
        <!-- Branding Section -->
        <div class="branding-section">
            <div class="branding-logo">
                <img src="<%= logoUrl %>" alt="<%= siteName %>">
            </div>
            <h1 class="branding-title"><%= siteName.replace('Rukchai Hongyen ', '') %></h1>
            <p class="branding-subtitle">
                <span data-th="ระบบการเรียนรู้ออนไลน์ พัฒนาทักษะบุคลากร" data-en="Online Learning System for Staff Development">
                    <%= currentLang === 'en' ? 'Online Learning System for Staff Development' : 'ระบบการเรียนรู้ออนไลน์ พัฒนาทักษะบุคลากร' %>
                </span>
            </p>
            <div class="branding-features">
                <div class="branding-feature">
                    <i class="fas fa-lock"></i>
                    <span data-th="รหัสผ่านที่ปลอดภัย" data-en="Secure Password">
                        <%= currentLang === 'en' ? 'Secure Password' : 'รหัสผ่านที่ปลอดภัย' %>
                    </span>
                </div>
                <div class="branding-feature">
                    <i class="fas fa-shield-alt"></i>
                    <span data-th="การเข้ารหัสข้อมูล" data-en="Data Encryption">
                        <%= currentLang === 'en' ? 'Data Encryption' : 'การเข้ารหัสข้อมูล' %>
                    </span>
                </div>
                <div class="branding-feature">
                    <i class="fas fa-key"></i>
                    <span data-th="เปลี่ยนรหัสผ่านง่าย" data-en="Easy Password Reset">
                        <%= currentLang === 'en' ? 'Easy Password Reset' : 'เปลี่ยนรหัสผ่านง่าย' %>
                    </span>
                </div>
            </div>
        </div>

        <!-- Form Section -->
        <div class="form-section">
            <div class="container">
                <!-- Language Selector -->
                <div class="language-selector">
                    <button id="auth-language-switcher" class="auth-language-button">
                        <span id="auth-current-language-text"><%= currentLang === 'en' ? 'EN' : 'TH' %></span>
                        <i class="fas fa-chevron-down" style="font-size: 0.7rem;"></i>
                    </button>
                    <div id="auth-language-menu" class="auth-language-dropdown">
                        <button class="auth-language-option <%= currentLang === 'th' ? 'active' : '' %>" data-lang="th">ไทย</button>
                        <button class="auth-language-option <%= currentLang === 'en' ? 'active' : '' %>" data-lang="en">English</button>
                    </div>
                </div>

                <!-- Header -->
                <div class="header">
                    <div class="logo">
                        <img src="<%= logoUrl %>" alt="<%= siteName %>">
                    </div>
                    <h1 class="title" data-th="รีเซ็ตรหัสผ่าน" data-en="Reset Password">
                        <%= currentLang === 'en' ? 'Reset Password' : 'รีเซ็ตรหัสผ่าน' %>
                    </h1>
                    <p class="subtitle" data-th="กรอกรหัสผ่านใหม่ของคุณ" data-en="Enter your new password">
                        <%= currentLang === 'en' ? 'Enter your new password' : 'กรอกรหัสผ่านใหม่ของคุณ' %>
                    </p>
                </div>

                <!-- Token Invalid Alert -->
                <% if (locals.tokenInvalid) { %>
                <div class="alert alert-warning show">
                    <i class="fas fa-exclamation-triangle"></i>
                    <div>
                        <span data-th="ลิงก์รีเซ็ตรหัสผ่านไม่ถูกต้องหรือหมดอายุแล้ว" data-en="Reset link is invalid or expired">
                            <%= currentLang === 'en' ? 'Reset link is invalid or expired' : 'ลิงก์รีเซ็ตรหัสผ่านไม่ถูกต้องหรือหมดอายุแล้ว' %>
                        </span>
                        <a href="/auth/forgot-password" style="margin-left: 0.5rem;" data-th="ขอลิงก์ใหม่" data-en="Request new link">
                            <%= currentLang === 'en' ? 'Request new link' : 'ขอลิงก์ใหม่' %>
                        </a>
                    </div>
                </div>
                <% } %>

                <!-- Error Alert -->
                <div id="error-alert" class="alert alert-error">
                    <i class="fas fa-exclamation-circle"></i>
                    <span id="error-text"></span>
                </div>

                <!-- Form -->
                <form id="reset-form" action="/auth/reset-password" method="POST">
                    <input type="hidden" name="token" value="<%= typeof token !== 'undefined' ? token : '' %>">

                    <div class="form-group">
                        <label for="password" class="label" data-th="รหัสผ่านใหม่" data-en="New Password">
                            <%= currentLang === 'en' ? 'New Password' : 'รหัสผ่านใหม่' %>
                        </label>
                        <div class="input-group">
                            <i class="fas fa-lock input-icon"></i>
                            <input type="password" id="password" name="password" class="input" required minlength="8"
                                   data-th-placeholder="กรอกรหัสผ่านใหม่" data-en-placeholder="Enter new password"
                                   placeholder="<%= currentLang === 'en' ? 'Enter new password' : 'กรอกรหัสผ่านใหม่' %>">
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="confirm_password" class="label" data-th="ยืนยันรหัสผ่านใหม่" data-en="Confirm New Password">
                            <%= currentLang === 'en' ? 'Confirm New Password' : 'ยืนยันรหัสผ่านใหม่' %>
                        </label>
                        <div class="input-group">
                            <i class="fas fa-lock input-icon"></i>
                            <input type="password" id="confirm_password" name="confirm_password" class="input" required minlength="8"
                                   data-th-placeholder="กรอกรหัสผ่านใหม่อีกครั้ง" data-en-placeholder="Re-enter new password"
                                   placeholder="<%= currentLang === 'en' ? 'Re-enter new password' : 'กรอกรหัสผ่านใหม่อีกครั้ง' %>">
                        </div>
                    </div>

                    <!-- Password Requirements -->
                    <div class="requirements-box">
                        <h4 data-th="ข้อกำหนดรหัสผ่าน:" data-en="Password Requirements:">
                            <%= currentLang === 'en' ? 'Password Requirements:' : 'ข้อกำหนดรหัสผ่าน:' %>
                        </h4>
                        <div id="length-check" class="requirement-item invalid">
                            <i class="fas fa-times"></i>
                            <span data-th="อย่างน้อย 8 ตัวอักษร" data-en="At least 8 characters">
                                <%= currentLang === 'en' ? 'At least 8 characters' : 'อย่างน้อย 8 ตัวอักษร' %>
                            </span>
                        </div>
                        <div id="uppercase-check" class="requirement-item invalid">
                            <i class="fas fa-times"></i>
                            <span data-th="มีตัวอักษรพิมพ์ใหญ่อย่างน้อย 1 ตัว" data-en="At least 1 uppercase letter">
                                <%= currentLang === 'en' ? 'At least 1 uppercase letter' : 'มีตัวอักษรพิมพ์ใหญ่อย่างน้อย 1 ตัว' %>
                            </span>
                        </div>
                        <div id="lowercase-check" class="requirement-item invalid">
                            <i class="fas fa-times"></i>
                            <span data-th="มีตัวอักษรพิมพ์เล็กอย่างน้อย 1 ตัว" data-en="At least 1 lowercase letter">
                                <%= currentLang === 'en' ? 'At least 1 lowercase letter' : 'มีตัวอักษรพิมพ์เล็กอย่างน้อย 1 ตัว' %>
                            </span>
                        </div>
                        <div id="number-check" class="requirement-item invalid">
                            <i class="fas fa-times"></i>
                            <span data-th="มีตัวเลขอย่างน้อย 1 ตัว" data-en="At least 1 number">
                                <%= currentLang === 'en' ? 'At least 1 number' : 'มีตัวเลขอย่างน้อย 1 ตัว' %>
                            </span>
                        </div>
                        <div id="match-check" class="requirement-item invalid">
                            <i class="fas fa-times"></i>
                            <span data-th="รหัสผ่านตรงกัน" data-en="Passwords match">
                                <%= currentLang === 'en' ? 'Passwords match' : 'รหัสผ่านตรงกัน' %>
                            </span>
                        </div>
                    </div>

                    <button type="submit" class="submit-btn" id="submit-btn" disabled>
                        <i class="fas fa-key"></i>
                        <span id="btn-text" data-th="รีเซ็ตรหัสผ่าน" data-en="Reset Password">
                            <%= currentLang === 'en' ? 'Reset Password' : 'รีเซ็ตรหัสผ่าน' %>
                        </span>
                        <span id="btn-loading" style="display: none;">
                            <div class="loading-spinner"></div>
                            <span data-th="กำลังรีเซ็ต..." data-en="Resetting...">
                                <%= currentLang === 'en' ? 'Resetting...' : 'กำลังรีเซ็ต...' %>
                            </span>
                        </span>
                    </button>
                </form>

                <div class="links">
                    <a href="/auth/login">
                        <i class="fas fa-arrow-left"></i>
                        <span data-th="กลับไปหน้าเข้าสู่ระบบ" data-en="Back to Sign In">
                            <%= currentLang === 'en' ? 'Back to Sign In' : 'กลับไปหน้าเข้าสู่ระบบ' %>
                        </span>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script>
        let currentLang = '<%= currentLang %>';

        const LanguageManager = {
            init() {
                const switcher = document.getElementById('auth-language-switcher');
                const menu = document.getElementById('auth-language-menu');
                const options = document.querySelectorAll('.auth-language-option');

                if (switcher && menu) {
                    switcher.addEventListener('click', (e) => {
                        e.stopPropagation();
                        menu.classList.toggle('open');
                    });
                    document.addEventListener('click', () => menu.classList.remove('open'));
                }

                options.forEach(opt => {
                    opt.addEventListener('click', () => this.changeLanguage(opt.dataset.lang));
                });
            },

            async changeLanguage(lang) {
                if (lang === currentLang) return;
                currentLang = lang;
                localStorage.setItem('ruxchai_language', lang);

                try {
                    await fetch('/language/switch', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ language: lang })
                    });
                } catch (e) {}

                this.updateUI();
                document.getElementById('auth-language-menu').classList.remove('open');
            },

            updateUI() {
                document.getElementById('auth-current-language-text').textContent = currentLang === 'en' ? 'EN' : 'TH';
                document.querySelectorAll('.auth-language-option').forEach(opt => {
                    opt.classList.toggle('active', opt.dataset.lang === currentLang);
                });
                document.querySelectorAll('[data-th][data-en]').forEach(el => {
                    el.textContent = currentLang === 'th' ? el.dataset.th : el.dataset.en;
                });
                document.querySelectorAll('[data-th-placeholder][data-en-placeholder]').forEach(el => {
                    el.placeholder = currentLang === 'th' ? el.dataset.thPlaceholder : el.dataset.enPlaceholder;
                });
                document.documentElement.lang = currentLang;
            }
        };

        document.addEventListener('DOMContentLoaded', function() {
            LanguageManager.init();

            const form = document.getElementById('reset-form');
            const submitBtn = document.getElementById('submit-btn');
            const btnText = document.getElementById('btn-text');
            const btnLoading = document.getElementById('btn-loading');
            const errorAlert = document.getElementById('error-alert');
            const errorText = document.getElementById('error-text');
            const password = document.getElementById('password');
            const confirmPassword = document.getElementById('confirm_password');

            const lengthCheck = document.getElementById('length-check');
            const uppercaseCheck = document.getElementById('uppercase-check');
            const lowercaseCheck = document.getElementById('lowercase-check');
            const numberCheck = document.getElementById('number-check');
            const matchCheck = document.getElementById('match-check');

            password.addEventListener('input', validatePassword);
            confirmPassword.addEventListener('input', validatePassword);

            function validatePassword() {
                const pwd = password.value;
                const confirmPwd = confirmPassword.value;

                updateCheck(lengthCheck, pwd.length >= 8);
                updateCheck(uppercaseCheck, /[A-Z]/.test(pwd));
                updateCheck(lowercaseCheck, /[a-z]/.test(pwd));
                updateCheck(numberCheck, /\d/.test(pwd));
                updateCheck(matchCheck, pwd && confirmPwd && pwd === confirmPwd);

                const allValid = lengthCheck.classList.contains('valid') &&
                                 uppercaseCheck.classList.contains('valid') &&
                                 lowercaseCheck.classList.contains('valid') &&
                                 numberCheck.classList.contains('valid') &&
                                 matchCheck.classList.contains('valid');

                submitBtn.disabled = !allValid;
            }

            function updateCheck(element, isValid) {
                const icon = element.querySelector('i');
                if (isValid) {
                    element.classList.remove('invalid');
                    element.classList.add('valid');
                    icon.className = 'fas fa-check';
                } else {
                    element.classList.remove('valid');
                    element.classList.add('invalid');
                    icon.className = 'fas fa-times';
                }
            }

            form.addEventListener('submit', async function(e) {
                e.preventDefault();

                if (password.value !== confirmPassword.value) {
                    showError(currentLang === 'en' ? 'Passwords do not match' : 'รหัสผ่านไม่ตรงกัน');
                    return;
                }

                submitBtn.disabled = true;
                btnText.style.display = 'none';
                btnLoading.style.display = 'flex';
                errorAlert.classList.remove('show');

                try {
                    const formData = new FormData(form);
                    const response = await fetch('/auth/reset-password', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            token: formData.get('token'),
                            password: formData.get('password'),
                            confirm_password: formData.get('confirm_password')
                        })
                    });

                    const data = await response.json();

                    if (data.success) {
                        window.location.href = '/auth/login?message=' + encodeURIComponent(
                            currentLang === 'en' ? 'Password reset successful. Please sign in.' : 'รีเซ็ตรหัสผ่านสำเร็จ กรุณาเข้าสู่ระบบ'
                        );
                    } else {
                        throw new Error(data.message || (currentLang === 'en' ? 'Error resetting password' : 'เกิดข้อผิดพลาดในการรีเซ็ตรหัสผ่าน'));
                    }
                } catch (error) {
                    showError(error.message);
                } finally {
                    submitBtn.disabled = false;
                    btnText.style.display = 'inline';
                    btnLoading.style.display = 'none';
                    validatePassword();
                }
            });

            function showError(message) {
                errorText.textContent = message;
                errorAlert.classList.add('show');
            }

            form.addEventListener('input', () => errorAlert.classList.remove('show'));
        });
    </script>
</body>
</html>
