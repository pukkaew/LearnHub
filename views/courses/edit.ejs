

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Header -->
    <div class="mb-8">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-3xl font-bold text-gray-900">
                    <i class="fas fa-edit mr-3 text-ruxchai-primary"></i>แก้ไขหลักสูตร
                </h1>
                <p class="mt-2 text-gray-600">แก้ไขข้อมูลหลักสูตร: <span id="course-title" class="font-medium"></span></p>
            </div>
            <div class="flex space-x-3">
                <a href="/courses" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm bg-white text-sm font-medium text-gray-700 hover:bg-gray-50">
                    <i class="fas fa-arrow-left mr-2"></i>กลับไปรายการ
                </a>
                <a id="view-course-link" href="#" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm bg-white text-sm font-medium text-gray-700 hover:bg-gray-50">
                    <i class="fas fa-eye mr-2"></i>ดูหลักสูตร
                </a>
            </div>
        </div>
    </div>

    <!-- Status Alert -->
    <div id="course-status-alert" class="hidden mb-6 rounded-md p-4">
        <div class="flex">
            <div class="flex-shrink-0">
                <i id="status-icon" class="fas fa-info-circle"></i>
            </div>
            <div class="ml-3">
                <p id="status-message" class="text-sm font-medium"></p>
            </div>
        </div>
    </div>

    <!-- Course Edit Form -->
    <div class="bg-white shadow rounded-lg">
        <form id="edit-course-form" class="p-6 space-y-6">
            <input type="hidden" id="course_id" name="course_id">

            <!-- Course Basic Information -->
            <div class="border-b border-gray-200 pb-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">
                    <i class="fas fa-info-circle mr-2 text-ruxchai-primary"></i>ข้อมูลพื้นฐาน
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="md:col-span-2">
                        <label for="course_name" class="block text-sm font-medium text-gray-700 mb-2">ชื่อหลักสูตร <span class="text-red-500">*</span></label>
                        <input type="text" id="course_name" name="course_name" required
                               class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                    </div>

                    <div>
                        <label for="course_code" class="block text-sm font-medium text-gray-700 mb-2">รหัสหลักสูตร <span class="text-red-500">*</span></label>
                        <input type="text" id="course_code" name="course_code" required
                               class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                    </div>

                    <div>
                        <label for="category_id" class="block text-sm font-medium text-gray-700 mb-2">หมวดหมู่ <span class="text-red-500">*</span></label>
                        <select id="category_id" name="category_id" required
                                class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                            <option value="">เลือกหมวดหมู่</option>
                        </select>
                    </div>

                    <div>
                        <label for="difficulty_level" class="block text-sm font-medium text-gray-700 mb-2">ระดับความยาก</label>
                        <select id="difficulty_level" name="difficulty_level"
                                class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                            <option value="Beginner">เริ่มต้น</option>
                            <option value="Intermediate">ปานกลาง</option>
                            <option value="Advanced">ขั้นสูง</option>
                        </select>
                    </div>

                    <div>
                        <label for="course_type" class="block text-sm font-medium text-gray-700 mb-2">ประเภทหลักสูตร</label>
                        <select id="course_type" name="course_type"
                                class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                            <option value="Required">บังคับ</option>
                            <option value="Elective">เลือกเรียน</option>
                            <option value="Recommended">แนะนำ</option>
                        </select>
                    </div>

                    <div>
                        <label for="language" class="block text-sm font-medium text-gray-700 mb-2">ภาษา</label>
                        <select id="language" name="language"
                                class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                            <option value="Thai">ภาษาไทย</option>
                            <option value="English">ภาษาอังกฤษ</option>
                            <option value="Both">ทั้งสองภาษา</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">ระยะเวลา</label>
                        <div class="grid grid-cols-2 gap-3">
                            <div>
                                <input type="number" id="duration_hours" name="duration_hours" min="0" max="500" placeholder="ชั่วโมง"
                                       class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                                <p class="text-xs text-gray-500 mt-1">ชั่วโมง</p>
                            </div>
                            <div>
                                <input type="number" id="duration_minutes" name="duration_minutes" min="0" max="59" placeholder="นาที"
                                       class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                                <p class="text-xs text-gray-500 mt-1">นาที</p>
                            </div>
                        </div>
                    </div>

                    <div class="md:col-span-2">
                        <label for="description" class="block text-sm font-medium text-gray-700 mb-2">คำอธิบายหลักสูตร</label>
                        <textarea id="description" name="description" rows="4"
                                  class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary"></textarea>
                    </div>
                </div>
            </div>

            <!-- Course Details -->
            <div class="border-b border-gray-200 pb-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">
                    <i class="fas fa-list-ul mr-2 text-ruxchai-primary"></i>รายละเอียดหลักสูตร
                </h3>
                <div class="space-y-6">
                    <!-- Learning Objectives -->
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">วัตถุประสงค์การเรียนรู้</label>
                        <div id="objectives-container" class="space-y-2">
                            <!-- Objectives will be loaded here -->
                        </div>
                        <button type="button" onclick="addObjective()" class="mt-2 inline-flex items-center px-3 py-1 border border-gray-300 rounded-md text-sm text-gray-700 bg-white hover:bg-gray-50">
                            <i class="fas fa-plus mr-2"></i>เพิ่มวัตถุประสงค์
                        </button>
                        <input type="hidden" id="learning_objectives" name="learning_objectives">
                    </div>

                    <!-- Prerequisite Knowledge -->
                    <div>
                        <label for="prerequisite_knowledge" class="block text-sm font-medium text-gray-700 mb-2">ความรู้พื้นฐานที่ต้องมี</label>
                        <textarea id="prerequisite_knowledge" name="prerequisite_knowledge" rows="3" placeholder="ระบุความรู้พื้นฐานที่ผู้เรียนควรมีก่อนเข้าเรียน"
                                  class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary"></textarea>
                    </div>

                    <!-- Target Audience -->
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div>
                            <label for="target_departments" class="block text-sm font-medium text-gray-700 mb-2">
                                <span class="inline-flex items-center">
                                    <span class="bg-blue-500 text-white rounded-full w-5 h-5 flex items-center justify-center text-xs mr-2">1</span>
                                    หน่วยงานเป้าหมาย
                                </span>
                                <span class="text-gray-400 text-xs ml-7">(เลือกก่อน เพื่อกรองตำแหน่ง)</span>
                            </label>
                            <select id="target_departments" name="target_departments[]" multiple size="10"
                                    class="w-full rounded-md border-2 border-gray-300 shadow-sm focus:border-blue-500 focus:ring-2 focus:ring-blue-200 p-2"
                                    style="height: auto;">
                                <option value="" disabled class="text-gray-400">กำลังโหลดข้อมูล...</option>
                            </select>
                            <div class="mt-2 flex items-center justify-between">
                                <p class="text-xs text-gray-600">
                                    <i class="fas fa-mouse-pointer text-blue-500"></i> <strong>คลิก</strong> เพื่อเลือก/ยกเลิก
                                </p>
                                <button type="button" id="clear-departments-btn"
                                        class="text-xs text-red-600 hover:text-red-800 hover:underline">
                                    <i class="fas fa-times-circle"></i> ล้างทั้งหมด
                                </button>
                            </div>
                        </div>

                        <div>
                            <label for="target_positions" class="block text-sm font-medium text-gray-700 mb-2">
                                <span class="inline-flex items-center">
                                    <span class="bg-green-500 text-white rounded-full w-5 h-5 flex items-center justify-center text-xs mr-2">2</span>
                                    ตำแหน่งเป้าหมาย
                                </span>
                                <span class="text-gray-400 text-xs ml-7">(ไม่เลือก = ทุกตำแหน่ง)</span>
                            </label>
                            <select id="target_positions" name="target_positions[]" multiple size="10"
                                    class="w-full rounded-md border-2 border-gray-300 shadow-sm focus:border-green-500 focus:ring-2 focus:ring-green-200 p-2"
                                    style="height: auto;">
                                <option value="" disabled class="text-gray-400">กำลังโหลดข้อมูล...</option>
                            </select>
                            <div class="mt-2 flex items-center justify-between">
                                <p class="text-xs text-gray-600">
                                    <i class="fas fa-filter text-green-500"></i> กรองตามหน่วยงานที่เลือก
                                </p>
                                <button type="button" id="clear-positions-btn"
                                        class="text-xs text-red-600 hover:text-red-800 hover:underline">
                                    <i class="fas fa-times-circle"></i> ล้างทั้งหมด
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Course Image -->
            <div class="border-b border-gray-200 pb-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">
                    <i class="fas fa-image mr-2 text-ruxchai-primary"></i>รูปภาพหลักสูตร
                </h3>
                <div class="flex items-center space-x-6">
                    <div class="flex-shrink-0">
                        <img id="course-image-preview" class="h-32 w-48 object-cover rounded-lg border-2 border-dashed border-gray-300"
                             src="/images/default-avatar.png" alt="Course Image Preview">
                    </div>
                    <div class="flex-1">
                        <label for="course_image" class="block text-sm font-medium text-gray-700 mb-2">อัปโหลดรูปภาพใหม่</label>
                        <input type="file" id="course_image" name="course_image" accept="image/*"
                               class="w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-ruxchai-primary file:text-white hover:file:bg-blue-700">
                        <p class="text-xs text-gray-500 mt-1">ขนาดแนะนำ: 800x600 px, ไฟล์ไม่เกิน 2MB</p>
                        <button type="button" id="remove-image" class="hidden mt-2 text-sm text-red-600 hover:text-red-800">
                            <i class="fas fa-trash mr-1"></i>ลบรูปภาพ
                        </button>
                    </div>
                </div>
            </div>

            <!-- Course Statistics -->
            <div class="border-b border-gray-200 pb-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">
                    <i class="fas fa-chart-bar mr-2 text-ruxchai-primary"></i>สถิติหลักสูตร
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                    <div class="bg-blue-50 rounded-lg p-4">
                        <div class="flex items-center">
                            <i class="fas fa-users text-blue-500 text-xl mr-3"></i>
                            <div>
                                <p class="text-sm font-medium text-gray-600">ผู้ลงทะเบียน</p>
                                <p id="total-enrollments" class="text-2xl font-bold text-blue-600">-</p>
                            </div>
                        </div>
                    </div>
                    <div class="bg-green-50 rounded-lg p-4">
                        <div class="flex items-center">
                            <i class="fas fa-graduation-cap text-green-500 text-xl mr-3"></i>
                            <div>
                                <p class="text-sm font-medium text-gray-600">จบหลักสูตรแล้ว</p>
                                <p id="completed-count" class="text-2xl font-bold text-green-600">-</p>
                            </div>
                        </div>
                    </div>
                    <div class="bg-yellow-50 rounded-lg p-4">
                        <div class="flex items-center">
                            <i class="fas fa-star text-yellow-500 text-xl mr-3"></i>
                            <div>
                                <p class="text-sm font-medium text-gray-600">คะแนนเฉลี่ย</p>
                                <p id="avg-score" class="text-2xl font-bold text-yellow-600">-</p>
                            </div>
                        </div>
                    </div>
                    <div class="bg-purple-50 rounded-lg p-4">
                        <div class="flex items-center">
                            <i class="fas fa-percentage text-purple-500 text-xl mr-3"></i>
                            <div>
                                <p class="text-sm font-medium text-gray-600">อัตราผ่าน</p>
                                <p id="completion-rate" class="text-2xl font-bold text-purple-600">-</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Course Settings -->
            <div class="border-b border-gray-200 pb-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">
                    <i class="fas fa-cogs mr-2 text-ruxchai-primary"></i>การตั้งค่าหลักสูตร
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label for="max_enrollments" class="block text-sm font-medium text-gray-700 mb-2">จำนวนผู้เรียนสูงสุด</label>
                        <input type="number" id="max_enrollments" name="max_enrollments" min="1"
                               class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                    </div>

                    <div>
                        <label for="passing_score" class="block text-sm font-medium text-gray-700 mb-2">คะแนนผ่าน (%)</label>
                        <input type="number" id="passing_score" name="passing_score" min="0" max="100" placeholder="เช่น 70"
                               class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                        <p class="text-xs text-gray-500 mt-1">ปล่อยว่างถ้าไม่ต้องการระบุ</p>
                    </div>

                    <div>
                        <label for="max_attempts" class="block text-sm font-medium text-gray-700 mb-2">ทำได้สูงสุด (ครั้ง)</label>
                        <input type="number" id="max_attempts" name="max_attempts" min="1" placeholder="เช่น 3"
                               class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                        <p class="text-xs text-gray-500 mt-1">ปล่อยว่างถ้าไม่ต้องการระบุ</p>
                    </div>

                    <div>
                        <label for="certificate_validity" class="block text-sm font-medium text-gray-700 mb-2">อายุใบประกาศนียบัตร (เดือน)</label>
                        <input type="number" id="certificate_validity" name="certificate_validity" min="1" placeholder="เช่น 12"
                               class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                        <p class="text-xs text-gray-500 mt-1">ปล่อยว่างถ้าไม่จำกัดอายุ</p>
                    </div>

                    <div>
                        <label for="enrollment_start" class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="far fa-calendar-alt mr-1"></i>วันเริ่มลงทะเบียน
                        </label>
                        <input type="text" id="enrollment_start" name="enrollment_start"
                               placeholder="เลือกวันที่และเวลา"
                               class="flatpickr-datetime w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary"
                               readonly>
                        <p class="text-xs text-gray-500 mt-1">คลิกเพื่อเลือกวันที่และเวลา</p>
                    </div>

                    <div>
                        <label for="enrollment_end" class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="far fa-calendar-alt mr-1"></i>วันสิ้นสุดการลงทะเบียน
                        </label>
                        <input type="text" id="enrollment_end" name="enrollment_end"
                               placeholder="เลือกวันที่และเวลา"
                               class="flatpickr-datetime w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary"
                               readonly>
                        <p class="text-xs text-gray-500 mt-1">คลิกเพื่อเลือกวันที่และเวลา</p>
                    </div>
                </div>

                <div class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-4">
                    <label class="inline-flex items-center">
                        <input type="checkbox" id="is_mandatory" name="is_mandatory"
                               class="rounded border-gray-300 text-ruxchai-primary shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                        <span class="ml-2 text-sm text-gray-700">หลักสูตรบังคับ</span>
                    </label>

                    <label class="inline-flex items-center">
                        <input type="checkbox" id="allow_certificate" name="allow_certificate"
                               class="rounded border-gray-300 text-ruxchai-primary shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                        <span class="ml-2 text-sm text-gray-700">ออกใบประกาศนียบัตร</span>
                    </label>

                    <label class="inline-flex items-center">
                        <input type="checkbox" id="is_public" name="is_public"
                               class="rounded border-gray-300 text-ruxchai-primary shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                        <span class="ml-2 text-sm text-gray-700">เปิดให้ลงทะเบียนได้</span>
                    </label>
                </div>
            </div>

            <!-- Status Management -->
            <div class="border-b border-gray-200 pb-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">
                    <i class="fas fa-toggle-on mr-2 text-ruxchai-primary"></i>การจัดการสถานะ
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label for="status" class="block text-sm font-medium text-gray-700 mb-2">สถานะหลักสูตร</label>
                        <select id="status" name="status"
                                class="w-full rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary">
                            <option value="Draft">ร่าง</option>
                            <option value="Published">เผยแพร่แล้ว</option>
                            <option value="Archived">เก็บถาวร</option>
                            <option value="Suspended">หยุดชั่วคราว</option>
                        </select>
                    </div>

                    <div>
                        <label for="instructor_name_display" class="block text-sm font-medium text-gray-700 mb-2">ผู้สอน</label>
                        <input type="text" id="instructor_name_display" readonly
                               class="w-full rounded-md border-gray-300 bg-gray-50 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary cursor-not-allowed">
                        <input type="hidden" id="instructor_id" name="instructor_id">
                        <p class="text-xs text-gray-500 mt-1">ผู้สอนไม่สามารถเปลี่ยนแปลงได้</p>
                    </div>
                </div>
            </div>

            <!-- Danger Zone -->
            <div class="border-b border-red-200 pb-6">
                <h3 class="text-lg font-medium text-red-900 mb-4">
                    <i class="fas fa-exclamation-triangle mr-2 text-red-600"></i>การดำเนินการที่เป็นอันตราย
                </h3>
                <div class="bg-red-50 border border-red-200 rounded-md p-4">
                    <div class="flex justify-between items-center">
                        <div>
                            <h4 class="text-sm font-medium text-red-800">ลบหลักสูตรถาวร</h4>
                            <p class="text-sm text-red-600 mt-1">การดำเนินการนี้ไม่สามารถยกเลิกได้ ข้อมูลทั้งหมดจะถูกลบถาวร</p>
                        </div>
                        <button type="button" onclick="confirmDeleteCourse()"
                                class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700">
                            <i class="fas fa-trash mr-2"></i>ลบหลักสูตร
                        </button>
                    </div>
                </div>
            </div>

            <!-- Form Actions -->
            <div class="flex justify-between pt-6">
                <div class="flex space-x-3">
                    <button type="button" onclick="previewCourse()"
                            class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm bg-white text-sm font-medium text-gray-700 hover:bg-gray-50">
                        <i class="fas fa-eye mr-2"></i>ตัวอย่าง
                    </button>
                    <button type="button" onclick="duplicateCourse()"
                            class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm bg-white text-sm font-medium text-gray-700 hover:bg-gray-50">
                        <i class="fas fa-copy mr-2"></i>คัดลอก
                    </button>
                </div>
                <div class="flex space-x-3">
                    <a href="/courses"
                       class="px-4 py-2 border border-gray-300 rounded-md shadow-sm bg-white text-sm font-medium text-gray-700 hover:bg-gray-50">
                        ยกเลิก
                    </a>
                    <button type="submit"
                            class="px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white ruxchai-gradient hover:opacity-90">
                        <i class="fas fa-save mr-2"></i>บันทึกการแก้ไข
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

<script>
let courseId;

document.addEventListener('DOMContentLoaded', async function() {
    // Extract course ID from URL: /courses/:course_id/edit
    // Example: /courses/1/edit → split('/') → ['', 'courses', '1', 'edit']
    courseId = window.location.pathname.split('/')[2]; // Get course_id (index 2)

    // Initialize flatpickr FIRST before loading data
    initializeFlatpickr();

    // Load dropdown data first, then load course data
    await Promise.all([
        loadCategories(),
        loadTargetPositions(),
        loadTargetDepartments()
    ]);
    await loadCourseData();

    setupImagePreview();
    setupFormSubmit();
});

async function loadCourseData() {
    try {
        const response = await fetch(`/courses/api/${courseId}`);
        const result = await response.json();

        if (result.success) {
            const course = result.data;
            populateForm(course);
            loadCourseStats();
        } else {
            showError('ไม่พบข้อมูลหลักสูตร');
            setTimeout(() => window.location.href = '/courses', 2000);
        }
    } catch (error) {
        showError('เกิดข้อผิดพลาดในการโหลดข้อมูล');
    }
}

// Learning objectives management - MUST be declared before populateForm()
let objectiveCount = 0;

function addObjective(value = '') {
    objectiveCount++;
    const container = document.getElementById('objectives-container');
    console.log('🎯 addObjective called:', { value, objectiveCount, containerExists: !!container });

    if (!container) {
        console.error('❌ objectives-container not found!');
        return;
    }

    const div = document.createElement('div');
    div.className = 'flex gap-2';
    div.innerHTML = `
        <input type="text" class="objective-input flex-1 rounded-md border-gray-300 shadow-sm focus:border-ruxchai-primary focus:ring-ruxchai-primary"
               placeholder="วัตถุประสงค์ข้อที่ ${objectiveCount}" value="${value}">
        <button type="button" onclick="removeObjective(this)" class="px-3 py-2 border border-red-300 rounded-md text-red-600 hover:bg-red-50">
            <i class="fas fa-trash"></i>
        </button>
    `;
    container.appendChild(div);
    console.log('✅ Objective added, container now has', container.children.length, 'objectives');
    updateObjectivesJSON();
}

function removeObjective(button) {
    button.parentElement.remove();
    updateObjectivesJSON();
}

function updateObjectivesJSON() {
    const inputs = document.querySelectorAll('.objective-input');
    const objectives = Array.from(inputs)
        .map(input => input.value.trim())
        .filter(val => val !== '');
    document.getElementById('learning_objectives').value = JSON.stringify(objectives);
}

// Helper function to format date for flatpickr (d/m/Y H:i) - kept for reference
function formatDateForFlatpickr(date) {
    if (!date) return '';

    const d = new Date(date);
    const day = String(d.getDate()).padStart(2, '0');
    const month = String(d.getMonth() + 1).padStart(2, '0');
    const year = d.getFullYear();
    const hours = String(d.getHours()).padStart(2, '0');
    const minutes = String(d.getMinutes()).padStart(2, '0');

    return `${day}/${month}/${year} ${hours}:${minutes}`;
}

// Helper function to format date for flatpickr ISO format (Y-m-d H:i:S)
function formatDateForISO(date) {
    if (!date) return '';

    const d = new Date(date);
    const year = d.getFullYear();
    const month = String(d.getMonth() + 1).padStart(2, '0');
    const day = String(d.getDate()).padStart(2, '0');
    const hours = String(d.getHours()).padStart(2, '0');
    const minutes = String(d.getMinutes()).padStart(2, '0');
    const seconds = String(d.getSeconds()).padStart(2, '0');

    return `${year}-${month}-${day} ${hours}:${minutes}:${seconds}`;
}

function populateForm(course) {
    document.getElementById('course_id').value = course.course_id;
    document.getElementById('course_name').value = course.course_name || course.title || '';
    document.getElementById('course_code').value = course.course_code || '';
    document.getElementById('description').value = course.description || '';
    document.getElementById('difficulty_level').value = course.difficulty_level || 'Beginner';

    // Map course_type values
    let courseType = course.course_type || 'Required';
    if (courseType === 'mandatory') courseType = 'Required';
    else if (courseType === 'elective') courseType = 'Elective';
    else if (courseType === 'recommended') courseType = 'Recommended';
    document.getElementById('course_type').value = courseType;

    // Map language values
    let language = course.language || 'Thai';
    if (language === 'th') language = 'Thai';
    else if (language === 'en') language = 'English';
    else if (language === 'both') language = 'Both';
    document.getElementById('language').value = language;

    document.getElementById('duration_hours').value = course.duration_hours || '';
    document.getElementById('duration_minutes').value = course.duration_minutes || '0';

    // Use max_students instead of max_enrollments
    document.getElementById('max_enrollments').value = course.max_enrollments || course.max_students || course.enrollment_limit || '';

    document.getElementById('passing_score').value = course.passing_score || '';
    document.getElementById('max_attempts').value = course.max_attempts || '';
    document.getElementById('certificate_validity').value = course.certificate_validity || '';
    document.getElementById('prerequisite_knowledge').value = course.prerequisite_knowledge || course.prerequisites_text || course.prerequisites || '';
    document.getElementById('status').value = course.status || 'Draft';

    // Set category - ไม่มี category_id ใน response จึงต้อง find จาก category_name
    if (course.category_name || course.category) {
        const categoryName = course.category_name || course.category;
        // Try to find matching option by text
        const categorySelect = document.getElementById('category_id');
        const options = categorySelect.options;
        for (let i = 0; i < options.length; i++) {
            if (options[i].textContent === categoryName) {
                options[i].selected = true;
                break;
            }
        }
    }

    // Set instructor (readonly field - show name, store ID)
    document.getElementById('instructor_id').value = course.instructor_id || '';
    document.getElementById('instructor_name_display').value =
        course.instructor_name || course.instructor_display_name || 'ไม่ระบุ';

    // Set learning objectives
    // Clear existing objectives first
    document.getElementById('objectives-container').innerHTML = '';
    objectiveCount = 0;

    if (course.learning_objectives || course.objectives) {
        const objectives = course.learning_objectives || course.objectives;
        console.log('📚 Learning objectives raw:', objectives);
        const objectivesArray = typeof objectives === 'string' ? JSON.parse(objectives) : objectives;
        console.log('📚 Learning objectives array:', objectivesArray);
        if (Array.isArray(objectivesArray) && objectivesArray.length > 0) {
            objectivesArray.forEach(obj => addObjective(obj));
            console.log(`✅ Added ${objectivesArray.length} learning objectives`);
        } else {
            console.log('⚠️ Learning objectives is empty array, adding default objective');
            addObjective(''); // Add one empty objective for user to fill
        }
    } else {
        console.log('⚠️ No learning objectives found, adding default objective');
        addObjective(''); // Add one empty objective for user to fill
    }

    // Set target audience (optional - may be empty)
    if (course.target_audience && typeof course.target_audience === 'object') {
        const targetAudience = typeof course.target_audience === 'string'
            ? JSON.parse(course.target_audience)
            : course.target_audience;

        // Select departments first (will trigger cascading)
        if (targetAudience.departments && Array.isArray(targetAudience.departments) && targetAudience.departments.length > 0) {
            targetAudience.departments.forEach(dept => {
                const option = document.querySelector(`#target_departments option[value="${dept}"]`);
                if (option) {
                    option.selected = true;
                    option.style.backgroundColor = '#3b82f6';
                    option.style.color = 'white';
                }
            });

            // Trigger cascading to filter positions
            document.getElementById('target_departments').dispatchEvent(new Event('change'));
        }

        // Then select positions (after cascading has filtered the list)
        setTimeout(() => {
            if (targetAudience.positions && Array.isArray(targetAudience.positions) && targetAudience.positions.length > 0) {
                targetAudience.positions.forEach(pos => {
                    const option = document.querySelector(`#target_positions option[value="${pos}"]`);
                    if (option) {
                        option.selected = true;
                        option.style.backgroundColor = '#3b82f6';
                        option.style.color = 'white';
                    }
                });
            }
        }, 100); // Small delay to ensure cascading completes
    }
    // If no target_audience data, leave selects empty (which is fine - optional field)

    // Set datetime fields - handle multiple field names
    // Use flatpickr's setDate() method instead of setting value directly
    if (course.enrollment_start || course.start_date) {
        const startDate = new Date(course.enrollment_start || course.start_date);
        const startElement = document.getElementById('enrollment_start');
        if (startElement._flatpickr) {
            startElement._flatpickr.setDate(startDate, false);
        } else {
            // Fallback if flatpickr not initialized yet
            startElement.value = formatDateForISO(startDate);
        }
    }
    if (course.enrollment_end || course.end_date) {
        const endDate = new Date(course.enrollment_end || course.end_date);
        const endElement = document.getElementById('enrollment_end');
        if (endElement._flatpickr) {
            endElement._flatpickr.setDate(endDate, false);
        } else {
            // Fallback if flatpickr not initialized yet
            endElement.value = formatDateForISO(endDate);
        }
    }

    // Set checkboxes
    document.getElementById('is_mandatory').checked = course.is_mandatory || false;
    document.getElementById('allow_certificate').checked = course.allow_certificate || false;
    document.getElementById('is_public').checked = course.is_public || false;

    // Set course image - use existing image or default (handle multiple field names)
    if (course.course_image) {
        document.getElementById('course-image-preview').src = course.course_image;
        document.getElementById('remove-image').classList.remove('hidden');
    } else if (course.thumbnail_image || course.thumbnail) {
        document.getElementById('course-image-preview').src = course.thumbnail_image || course.thumbnail;
        document.getElementById('remove-image').classList.remove('hidden');
    } else {
        document.getElementById('course-image-preview').src = '/images/default-avatar.png';
    }

    // Update page elements
    document.getElementById('course-title').textContent = course.course_name;
    document.getElementById('view-course-link').href = `/courses/${course.course_id}`;

    // Show status alert
    showStatusAlert(course.status);
}

function showStatusAlert(status) {
    const alert = document.getElementById('course-status-alert');
    const icon = document.getElementById('status-icon');
    const message = document.getElementById('status-message');

    alert.classList.remove('hidden', 'bg-yellow-50', 'bg-green-50', 'bg-red-50', 'bg-gray-50');

    switch (status) {
        case 'Draft':
            alert.classList.add('bg-yellow-50');
            icon.className = 'fas fa-exclamation-triangle text-yellow-400';
            message.textContent = 'หลักสูตรนี้อยู่ในสถานะร่าง ยังไม่เปิดให้ผู้ใช้ลงทะเบียน';
            message.className = 'text-sm font-medium text-yellow-800';
            break;
        case 'Published':
            alert.classList.add('bg-green-50');
            icon.className = 'fas fa-check-circle text-green-400';
            message.textContent = 'หลักสูตรนี้ถูกเผยแพร่แล้ว ผู้ใช้สามารถลงทะเบียนได้';
            message.className = 'text-sm font-medium text-green-800';
            break;
        case 'Archived':
            alert.classList.add('bg-gray-50');
            icon.className = 'fas fa-archive text-gray-400';
            message.textContent = 'หลักสูตรนี้ถูกเก็บถาวรแล้ว ไม่สามารถลงทะเบียนใหม่ได้';
            message.className = 'text-sm font-medium text-gray-800';
            break;
        case 'Suspended':
            alert.classList.add('bg-red-50');
            icon.className = 'fas fa-pause-circle text-red-400';
            message.textContent = 'หลักสูตรนี้ถูกหยุดชั่วคราว ไม่สามารถลงทะเบียนได้';
            message.className = 'text-sm font-medium text-red-800';
            break;
    }
}

async function loadCourseStats() {
    try {
        const response = await fetch(`/courses/api/${courseId}/stats`);
        const result = await response.json();

        if (result.success) {
            const stats = result.data;
            document.getElementById('total-enrollments').textContent = stats.total_enrollments || 0;
            document.getElementById('completed-count').textContent = stats.completed_count || 0;
            document.getElementById('avg-score').textContent = stats.avg_score ? `${stats.avg_score.toFixed(1)}%` : 'N/A';
            document.getElementById('completion-rate').textContent = stats.completion_rate ? `${stats.completion_rate.toFixed(1)}%` : 'N/A';
        }
    } catch (error) {
        console.error('Error loading course stats:', error);
    }
}

async function loadCategories() {
    try {
        const response = await fetch('/courses/api/categories');
        const result = await response.json();

        if (result.success) {
            const select = document.getElementById('category_id');
            result.data.forEach(category => {
                const option = document.createElement('option');
                option.value = category.category_id;
                option.textContent = category.category_name;
                select.appendChild(option);
            });
        }
    } catch (error) {
        console.error('Error loading categories:', error);
    }
}

// Removed loadInstructors() - instructor is now a readonly field

// Store all positions for filtering
let allPositions = [];

async function loadTargetPositions() {
    try {
        const response = await fetch('/courses/api/target-positions');
        const result = await response.json();

        if (result.success && result.data) {
            allPositions = result.data; // Store for cascading
            const select = document.getElementById('target_positions');
            select.innerHTML = ''; // Clear loading message

            // Add all positions initially
            result.data.forEach(position => {
                const option = document.createElement('option');
                option.value = position.position_id;
                option.textContent = position.position_name;
                option.dataset.unitId = position.unit_id || '';
                option.dataset.level = position.level || '';
                select.appendChild(option);
            });

            console.log(`✅ Loaded ${result.data.length} positions`);
        }
    } catch (error) {
        console.error('Error loading target positions:', error);
    }
}

async function loadTargetDepartments() {
    try {
        const response = await fetch('/courses/api/target-departments');
        const result = await response.json();

        if (result.success && result.data) {
            const select = document.getElementById('target_departments');
            select.innerHTML = ''; // Clear loading message
            result.data.forEach(department => {
                const option = document.createElement('option');
                option.value = department.unit_id;
                option.textContent = department.unit_name_th || department.name;
                option.dataset.level = department.level_id || '';
                select.appendChild(option);
            });

            console.log(`✅ Loaded ${result.data.length} departments`);

            // Setup cascading and enhanced multi-select after loading
            setupEnhancedMultiSelect();
            setupCascadingDropdown();
        }
    } catch (error) {
        console.error('Error loading target departments:', error);
    }
}

// Update objectives on input change (moved from duplicate section)
document.addEventListener('input', function(e) {
    if (e.target.classList.contains('objective-input')) {
        updateObjectivesJSON();
    }
});

function setupImagePreview() {
    document.getElementById('course_image').addEventListener('change', function(e) {
        const file = e.target.files[0];
        if (file) {
            const reader = new FileReader();
            reader.onload = function(e) {
                document.getElementById('course-image-preview').src = e.target.result;
                document.getElementById('remove-image').classList.remove('hidden');
            };
            reader.readAsDataURL(file);
        }
    });

    document.getElementById('remove-image').addEventListener('click', function() {
        document.getElementById('course-image-preview').src = '/images/default-avatar.png';
        document.getElementById('course_image').value = '';
        this.classList.add('hidden');
    });
}

function setupFormSubmit() {
    document.getElementById('edit-course-form').addEventListener('submit', async function(e) {
        e.preventDefault();

        const formData = new FormData(this);

        // Collect target positions (multiple select)
        const targetPositions = Array.from(document.getElementById('target_positions').selectedOptions)
            .map(option => option.value);

        // Collect target departments (multiple select)
        const targetDepartments = Array.from(document.getElementById('target_departments').selectedOptions)
            .map(option => option.value);

        const courseData = {
            course_name: formData.get('course_name'),
            course_code: formData.get('course_code'),
            description: formData.get('description'),
            category_id: formData.get('category_id'),
            difficulty_level: formData.get('difficulty_level'),
            course_type: formData.get('course_type'),
            language: formData.get('language'),
            duration_hours: formData.get('duration_hours'),
            duration_minutes: formData.get('duration_minutes'),
            prerequisite_knowledge: formData.get('prerequisite_knowledge'),
            learning_objectives: formData.get('learning_objectives'), // Already JSON from updateObjectivesJSON()
            target_positions: targetPositions,
            target_departments: targetDepartments,
            max_enrollments: formData.get('max_enrollments'),
            passing_score: formData.get('passing_score'),
            max_attempts: formData.get('max_attempts'),
            certificate_validity: formData.get('certificate_validity'),
            enrollment_start: formData.get('enrollment_start'),
            enrollment_end: formData.get('enrollment_end'),
            is_mandatory: formData.get('is_mandatory') ? true : false,
            allow_certificate: formData.get('allow_certificate') ? true : false,
            is_public: formData.get('is_public') ? true : false,
            status: formData.get('status'),
            instructor_id: formData.get('instructor_id')
        };

        try {
            const response = await fetch(`/courses/api/${courseId}`, {
                method: 'PUT',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(courseData)
            });

            const result = await response.json();

            if (result.success) {
                // Upload new image if provided
                const imageFile = formData.get('course_image');
                if (imageFile && imageFile.size > 0) {
                    const imageFormData = new FormData();
                    imageFormData.append('course_image', imageFile);

                    await fetch(`/courses/api/${courseId}/image`, {
                        method: 'POST',
                        body: imageFormData
                    });
                }

                showSuccess('บันทึกการแก้ไขเรียบร้อยแล้ว');
                // Redirect to course detail page after 500ms
                setTimeout(() => {
                    window.location.href = `/courses/${courseId}`;
                }, 500);
            } else {
                showError(result.message || 'เกิดข้อผิดพลาดในการบันทึก');
            }
        } catch (error) {
            showError('เกิดข้อผิดพลาดในการเชื่อมต่อ');
        }
    });
}

function previewCourse() {
    window.open(`/courses/${courseId}`, '_blank');
}

async function duplicateCourse() {
    if (confirm('คุณต้องการคัดลอกหลักสูตรนี้หรือไม่?')) {
        try {
            const response = await fetch(`/courses/api/${courseId}/duplicate`, {
                method: 'POST'
            });

            const result = await response.json();

            if (result.success) {
                showSuccess('คัดลอกหลักสูตรเรียบร้อยแล้ว');
                setTimeout(() => {
                    window.location.href = `/courses/${result.data.course_id}/edit`;
                }, 1500);
            } else {
                showError(result.message || 'เกิดข้อผิดพลาดในการคัดลอก');
            }
        } catch (error) {
            showError('เกิดข้อผิดพลาดในการเชื่อมต่อ');
        }
    }
}

async function confirmDeleteCourse() {
    const courseName = document.getElementById('course_name').value;

    if (confirm(`คุณแน่ใจหรือไม่ที่จะลบหลักสูตร "${courseName}"?\n\nการดำเนินการนี้ไม่สามารถยกเลิกได้ และจะลบข้อมูลทั้งหมดที่เกี่ยวข้อง`)) {
        try {
            const response = await fetch(`/courses/api/${courseId}`, {
                method: 'DELETE'
            });

            const result = await response.json();

            if (result.success) {
                showSuccess('ลบหลักสูตรเรียบร้อยแล้ว');
                setTimeout(() => {
                    window.location.href = '/courses';
                }, 1500);
            } else {
                showError(result.message || 'เกิดข้อผิดพลาดในการลบ');
            }
        } catch (error) {
            showError('เกิดข้อผิดพลาดในการเชื่อมต่อ');
        }
    }
}

// Enhanced multi-select: Click to toggle selection
function setupEnhancedMultiSelect() {
    const selects = ['target_departments', 'target_positions'];

    selects.forEach(selectId => {
        const select = document.getElementById(selectId);
        if (!select) return;

        // Click to toggle (without needing Ctrl)
        select.addEventListener('mousedown', function(e) {
            if (e.target.tagName === 'OPTION' && !e.target.disabled) {
                e.preventDefault();

                // Toggle selection
                e.target.selected = !e.target.selected;

                // Apply blue color to selected options
                if (e.target.selected) {
                    e.target.style.backgroundColor = '#3b82f6';
                    e.target.style.color = 'white';
                } else {
                    e.target.style.backgroundColor = '';
                    e.target.style.color = '';
                }

                // Trigger change event for cascading
                select.dispatchEvent(new Event('change'));
            }
        });

        // Prevent default selection behavior
        select.addEventListener('click', function(e) {
            e.preventDefault();
        });
    });

    // Clear buttons
    const clearDeptBtn = document.getElementById('clear-departments-btn');
    const clearPosBtn = document.getElementById('clear-positions-btn');

    if (clearDeptBtn) {
        clearDeptBtn.addEventListener('click', function() {
            const deptSelect = document.getElementById('target_departments');
            if (deptSelect) {
                // Deselect all options and clear colors
                Array.from(deptSelect.options).forEach(opt => {
                    opt.selected = false;
                    opt.style.backgroundColor = '';
                    opt.style.color = '';
                });
                // Trigger change for cascading
                deptSelect.dispatchEvent(new Event('change'));
                console.log('✅ Cleared all departments');
            }
        });
    }

    if (clearPosBtn) {
        clearPosBtn.addEventListener('click', function() {
            const posSelect = document.getElementById('target_positions');
            if (posSelect) {
                // Deselect all options and clear colors
                Array.from(posSelect.options).forEach(opt => {
                    opt.selected = false;
                    opt.style.backgroundColor = '';
                    opt.style.color = '';
                });
                console.log('✅ Cleared all positions');
            }
        });
    }
}

// Setup cascading dropdown
function setupCascadingDropdown() {
    const deptSelect = document.getElementById('target_departments');
    if (!deptSelect) return;

    // Listen to department changes
    deptSelect.addEventListener('change', filterPositionsByDepartment);
}

// Filter positions based on selected departments
function filterPositionsByDepartment() {
    const deptSelect = document.getElementById('target_departments');
    const posSelect = document.getElementById('target_positions');

    if (!deptSelect || !posSelect || allPositions.length === 0) return;

    const selectedDepts = Array.from(deptSelect.selectedOptions).map(opt => parseInt(opt.value));

    // Store currently selected positions before filtering
    const currentlySelectedPositions = Array.from(posSelect.selectedOptions).map(opt => parseInt(opt.value));

    // Clear current positions
    posSelect.innerHTML = '';

    if (selectedDepts.length === 0) {
        // No department selected → show all positions
        allPositions.forEach(position => {
            const option = document.createElement('option');
            option.value = position.position_id;
            option.textContent = position.position_name;
            option.dataset.unitId = position.unit_id || '';

            // Re-select if it was selected before
            if (currentlySelectedPositions.includes(position.position_id)) {
                option.selected = true;
                option.style.backgroundColor = '#3b82f6';
                option.style.color = 'white';
            }

            posSelect.appendChild(option);
        });
        console.log('✅ Showing all positions (no department filter)');
    } else {
        // Filter positions by selected departments
        const filteredPositions = allPositions.filter(p =>
            p.unit_id && selectedDepts.includes(parseInt(p.unit_id))
        );

        if (filteredPositions.length === 0) {
            posSelect.innerHTML = '<option value="" disabled>ไม่มีตำแหน่งในหน่วยงานที่เลือก</option>';
            console.log('⚠️ No positions found for selected departments');
        } else {
            filteredPositions.forEach(position => {
                const option = document.createElement('option');
                option.value = position.position_id;
                option.textContent = position.position_name;
                option.dataset.unitId = position.unit_id || '';

                // Re-select if it was selected before AND still in filtered list
                if (currentlySelectedPositions.includes(position.position_id)) {
                    option.selected = true;
                    option.style.backgroundColor = '#3b82f6';
                    option.style.color = 'white';
                }

                posSelect.appendChild(option);
            });
            console.log(`✅ Filtered to ${filteredPositions.length} positions for selected departments`);
        }
    }
}
</script>

<!-- Flatpickr CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">

<!-- Flatpickr JS -->
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/th.js"></script>
<script>
// Initialize Flatpickr for datetime fields (same as create page)
function initializeFlatpickr() {
    // Initialize Flatpickr for enrollment start date
    flatpickr("#enrollment_start", {
        enableTime: true,
        dateFormat: "Y-m-d H:i:S",  // ISO format for backend
        altInput: true,              // Show alternative format to user
        altFormat: "d/m/Y H:i",      // Thai format for display
        time_24hr: true,
        minDate: "today",
        locale: {
            firstDayOfWeek: 1
        },
        onChange: function(selectedDates, dateStr, instance) {
            // Update minDate for enrollment_end
            const endPicker = document.getElementById('enrollment_end')._flatpickr;
            if (endPicker && selectedDates.length > 0) {
                endPicker.set('minDate', selectedDates[0]);
            }
        }
    });

    // Initialize Flatpickr for enrollment end date
    flatpickr("#enrollment_end", {
        enableTime: true,
        dateFormat: "Y-m-d H:i:S",  // ISO format for backend
        altInput: true,              // Show alternative format to user
        altFormat: "d/m/Y H:i",      // Thai format for display
        time_24hr: true,
        minDate: "today",
        locale: {
            firstDayOfWeek: 1
        }
    });

    console.log('✅ DateTime pickers initialized');
}
</script>